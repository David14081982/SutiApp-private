# Fuentes de verdad

## Corte ADR-083 — visibilidad del catálogo financiero

`finance_catalog_presentation` es la única autoridad de presentación administrable para la pantalla Finanzas: habilitación, orden, títulos/subtítulos de sección y label/tagline de producto. `finCatStore` la proyecta sobre la estructura/rutas versionadas en código; no decide elegibilidad, tasas, fondos, reglas, importes ni cálculos.

La pantalla aplica `enabled !== false` antes de la elegibilidad financiera. Un producto oculto no puede reaparecer por perfil, recomendación o acceso rápido; una sección sin productos visibles se omite. Membresías conserva su autoridad separada y explícita. Si la presentación no carga, el catálogo falla cerrado con reintento visible y sin `DATA`, mock, `localStorage` o default productivo.

## Corte ADR-082 — notificaciones derivadas de autoridades reales

La pantalla Notificaciones y el badge no tienen tabla maestra propia. Su única fuente activa es la proyección self-only `list_self_marketplace_quote_notifications()` sobre cotizaciones reales posteriores al corte en `program_requests`; `DATA.notifs`, mocks y almacenamiento del navegador no participan. Una cotización `submitted` puede mostrarse como estado informativo y una respuesta `approved` es el único evento no leído actual.

`program_requests.seen_at` conserva el acuse durable del afiliado para la respuesta de cotización. `mark_marketplace_quote_seen` deriva `get_effective_affiliate_id()`, sólo marca una cotización propia completada y mantiene compatibilidad con la autoridad histórica `marketplace_quote_requests`; anónimo y cross-user quedan denegados. Workflow/tracking, documentos, membresías, solicitudes generales, programas y beneficios fueron inventariados pero no producen avisos mientras no exista en su propia autoridad un contrato durable de evento/visto. No se inventan filas ni se crea una tabla `notifications`.

## Corte ADR-081 — depósito bancario de Suti Préstamo

`affiliate_bank_accounts` permanece como única autoridad mutable de las cuentas del afiliado. `account_number` conserva semántica de cuenta y `card_number` conserva semántica separada de tarjeta; elegir una cuenta para el préstamo no modifica `is_primary`. Depósito lista exclusivamente por `list_current_deposit_accounts()`, que deriva el afiliado efectivo aunque el actor tenga permisos Admin globales. El writer `save_affiliate_deposit_account` deriva el mismo contexto y el titular server-side, valida banco, tarjeta de 16 dígitos y CLABE de 18 dígitos con checksum, y audita sin guardar números completos en metadata.

`affiliates.notification_phone` es el celular actual confirmado. `phone_raw` conserva exclusivamente el perfil histórico/importado y puede proyectarse como sugerencia explícita; nunca se sobrescribe ni funciona como autoridad productiva paralela. Lectura y confirmación pasan por RPC autenticada y auditada.

`program_requests` sigue siendo la autoridad de la solicitud. `loan_request_deposit_snapshots` es evidencia privada e inmutable de banco, titular, tarjeta, CLABE y celular al confirmar; carece de grants browser y se crea atómicamente por el writer service-only. `financial_submission_snapshot` y la auditoría sólo conservan máscaras/últimos cuatro. Una cuenta ajena, incompleta o cambiada falla cerrada y crea cero solicitudes parciales.

## Corte ADR-078 — plataforma central de requisitos documentales

`document_types` es el catálogo único de tipos; `program_document_requirements` continúa como única autoridad de configuración y ahora identifica el destino mediante `scope_type + scope_key` para `PROGRAM`, `COMPANY`, `PRODUCT`, `SERVICE` y `MEMBERSHIP`. No existe otra tabla de requisitos por pantalla. `resolve_effective_document_requirements` valida la entidad server-side y resuelve requisitos propios, herencia `COMPANY → PRODUCT`, adiciones, exclusiones y orden. `SERVICE` está soportado por contrato, pero no expone destinos mientras no exista una entidad productiva de servicio autorizada.

Las escrituras de catálogo y reglas se realizan exclusivamente por RPC administrativa con `documents.write`, motivo y `document_configuration_audit_log`; las escrituras directas del navegador están revocadas. `document_requirements_snapshot` congela los requisitos efectivos de cada nueva `program_request`; las cinco solicitudes históricas anteriores conservan `NULL` y nunca se reinterpretan con la configuración vigente.

`UnifiedDocumentPhase` es el único componente de autoservicio para presentar, reutilizar, capturar, adjuntar, previsualizar y reemplazar documentos en préstamo, membresía, programa y producto. Sigue consumiendo los contratos aislados de ADR-077: expediente vigente en `affiliate_documents`, evidencia enviada en `request_documents`, archivos físicos privados en `private_assets/private-assets` y firma individual derivada. La referencia HTML del propietario define presentación/interacción, pero no es fuente de datos ni introduce `DATA`, base64 persistente o `localStorage`.

## Corte ADR-076 — decisiones administrativas de solicitudes financieras

`program_requests` permanece como autoridad única de la solicitud y su estado; `program_requests.notes` conserva la nota original del solicitante. `program_request_admin_events` es la bitácora histórica append-only de comentarios, revisión, rechazo, cancelación y aprobación administrativa. Sus writers son `record_program_request_admin_action` para acciones browser autorizadas y la sobrecarga service-only de `approve_financial_program_request` para registrar aprobación en la misma transacción que el snapshot contractual. No existe store, mock, `DATA`, browser storage ni fallback productivo.

`request_documents` es la evidencia exacta de los archivos vinculados al envío. `affiliate_documents` + `private_assets` + el objeto en `private-assets` siguen siendo el expediente vigente y pueden proyectarse en una sección separada, pero no completan ni reconstruyen solicitudes históricas sin vínculos. Las URLs firmadas son derivados temporales creados al abrir y nunca autoridad.

La aprobación conserva el handoff append-only posterior hacia Google como una frontera legacy separada y explícita. Comentarios, revisión, rechazo y cancelación sólo escriben Supabase; no llaman Google ni alteran reglas, cálculos, tasas, saldos, amortizaciones o conciliaciones.

## Corte ADR-071 — Admin Afiliados

`public.affiliates` continúa como única autoridad productiva del padrón y expediente de identidad; el módulo Admin Afiliados no crea una tabla maestra paralela. Las altas administrativas escriben en esa misma tabla con `record_origin=ADMIN_AFFILIATES`, sin inventar `source_row_ordinal` ni `source_file_hash`. Las importaciones históricas conservan `record_origin=HISTORICAL_IMPORT` y sus coordenadas inmutables.

El frontend sólo usa `AdminAffiliatesRepository` y las RPC `list_admin_affiliates`, `get_admin_affiliate_workbench`, `find_admin_affiliate_duplicates`, `create_admin_affiliate`, `update_admin_affiliate`, `change_admin_affiliate_status` y `register_admin_affiliate_document`. Los writers de identidad exigen `affiliates.write`, versión optimista, motivo y auditoría durable en `affiliate_admin_events`; no existe DELETE productivo. La carga documental exige `documents.write`, usa `affiliate_documents` + `private_assets` + bucket privado `private-assets`, conserva todo `VERIFIED` inmutable y audita actor/motivo en `sensitive_change_audit`. Auth sigue siendo una autoridad separada y nullable. Expediente y solicitudes permanecen en sus tablas/workbenches canónicos.

La exportación Excel reutiliza `DataExportRepository → data-exports`. El XLSX es una proyección temporal `no-store`, exige `data_exports.read`, usa columnas/filtros allowlisted y registra sólo metadatos en `data_export_audit_log`; nunca se vuelve fuente de verdad ni se guarda en Storage.

## Índice técnico de arquitectura — ADR-058

El Architecture Registry es `DERIVED_TECHNICAL_INDEX`, no fuente productiva. Su origen único es el repo vigente (código, migrations y documentación normativa); `scripts/generate-architecture-registry.py` es su único escritor reproducible y `sutiapp-architecture-navigator` su lector operativo. `architecture-overrides.json` solo declara semántica no deducible y nunca prevalece ante una contradicción. No existe fallback runtime ni copia de datos de usuario.

## Corte ADR-065 — criterios financieros Supabase

| Dato/capacidad | Autoridad única | Consumidor/escritor |
|---|---|---|
| Elegibilidad, categoría, sindicato, fondo, tasa, monto, plazo y fecha | Supabase `financial_programs` + `financial_funds` + `financial_rules` | RPC service-only `get_financial_runtime_rules`; Admin mediante RPC auditadas |
| Modo de visibilidad SutiApp | Supabase `financial_rules.visibility_mode` | Admin autorizado mediante RPC; nunca escritura Google |
| Política AUTO y estado efectivo | Edge `financial-legacy`, derivado de `available_on` + `visibility_mode` Supabase + fecha de negocio Hermosillo | Simulador y Admin; el estado efectivo sigue siendo derivado |
| Autorización y auditoría administrativa | Supabase roles/permisos + `financial_configuration_audit` | RPC `SECURITY DEFINER` con permiso granular; nunca autoriza por UI |

El corte importó exactamente A/B/C/D/E/F/H/N/P; G/I/J/K/L/M/O quedaron fuera. La columna L es `OUT OF SCOPE / AUXILIARY LEGACY CALCULATION`. Google `Criterios de fondos` queda como procedencia histórica sin lectura ni escritura runtime; el writer OAuth anterior deja de ser autoridad. El batch activo tiene 146 reglas, 35 fondos, 3 programas y hash `174F940E195DE5DAE595AAF798CC1B49976AA899E76D6CF141FB9D711A6E9C8A`.

## Corte ADR-051 — préstamo flexible y asistencia

| Dato/capacidad | Autoridad única | Consumidor/escritor |
|---|---|---|
| Fondo, tasa, monto máximo, máximo de pagos y reglas | Supabase `financial_programs` + `financial_funds` + `financial_rules` | Edge/RPC leen el batch activo; Admin escribe por RPC auditadas |
| Sugerencias 6/12/18/24 y rango personalizado mínimo 1 | Supabase `loan_term_policy` | RPC autenticada → Edge; sin lectura directa frontend |
| Cotización derivada | Resolver server-side certificado sobre reglas Supabase + perfil/política Supabase | Repository/StepSimulator; el frontend no calcula |
| Contexto asistido | Supabase `impersonation_sessions` | RPC backend con TTL, motivo y no anidamiento |
| Alta inicial | Supabase `program_requests` | RPC y trigger de auditoría actor/contexto |
| Nómina informativa declarada | Supabase `affiliate_payroll_declarations` | RPC propia o asistida; nunca talón oficial |

## Foto de perfil del afiliado — corte global 2026-08-22

La autoridad runtime es `public.affiliate_files` con `file_key=profile_photo`, `source_column=Photo`, columna `DK`, relación a `private_assets` y objeto en el bucket privado `private-assets`. `AffiliateRepository.getProfilePhoto()` es el único lector frontend; TopBar, Perfil, Credencial y Admin autorizado consumen su proyección en memoria. El importador MASTER ASSET EVACUATION es el escritor histórico; cualquier cambio futuro de foto requiere writer Supabase autorizado y otra H.

Estado: `PROFILE PHOTO CUTOVER ACTIVE` — 487 relaciones inequívocas y 460 afiliados sin foto. La URL firmada dura una hora y su caché por principal existe solo en memoria durante 50 minutos. Placeholder/iniciales es el único estado sin foto o ante error de imagen; no hay fallback Glide, `DATA`, mock ni `localStorage`.

`UNRESOLVED` significa que H-000 no tiene evidencia suficiente para designar autoridad. `SOURCE OF TRUTH CONFLICT` significa que hay dos o más candidatos activos y no se elegirá uno por iniciativa del agente.

| Dominio | Fuente autoritativa actual | Fuente autoritativa futura | Lectores permitidos | Escritores permitidos | Estado de migración | Legacy protegido | Notas |
|---|---|---|---|---|---|---|---|
| Afiliados / identidad y expediente actual | RESOLVED — Supabase `public.affiliates`, inicializada por H-004 desde el Excel maestro reconciliado 947/947 | Supabase `public.affiliates` | `AffiliateRepository`; `AffiliateAuth`; Admin Afiliados; `financial-legacy` consume sólo el contexto financiero actual | Importador H-004; seed exact-hash de categoría/sindicato una sola vez; `claim_affiliate_identity()` limitado; Admin por `affiliates.write` + RPC auditada | ADR-043 + ADR-071 ACTIVE / INITIAL SEED APPLIED | Sí, puente por `numero_control` | Batch `82179501-f85b-50c3-b7ae-7f6998852163`: 947/947, 931 categorías, 770 sindicatos, 16/177 NULL, 0 inferencias y 0 mismatches. Altas Admin usan `record_origin=ADMIN_AFFILIATES` sin coordenadas históricas. Excel SHA-256 `F4BA18ABE82B148ED65737DB16074303627F96D37FA6F9F025E0A10649BD9591` queda sólo como seed/procedencia, nunca fallback ni sync. |
| Autenticación de afiliados | Supabase Auth; tres cuentas de prueba controladas con vínculo exacto `auth.uid()` → `affiliates.auth_user_id` | Mismo | `AffiliateAuth`, cliente Supabase y `AffiliateRepository` | Autoservicio limitado mediante `claim_affiliate_identity`; procesos administrativos autorizados | `PASS_WITH_DEFERRED_PRODUCTION_ACTIVATION_TEST`; `AUTH-PROD-ACTIVATION-CERT` pendiente online; sin creación masiva | Sí | Supabase Auth es la única autoridad de sesión. Arquitectura, login, recovery, sesión, RLS e aislamiento están certificados. El correo/callback positivo se ejecutará solo con URL y dominio productivos definitivos; Fase 1 no queda `CLOSED` hasta entonces. |
| Autenticación administrativa | RESOLVED — Supabase Auth | Mismo | `AffiliateAuth`, `AdminRepository`, `AdminScreen` y RLS | Supabase Auth; credenciales gestionadas fuera del frontend | H-008 ACTIVE; H005_TEST autorizado, H005_TEST2/3 denegados | No | Una persona puede ser afiliado, admin o ambos. `localStorage`, viewer y el gate simulado no autentican ni autorizan administración. |
| Roles y permisos técnicos administrativos | RESOLVED — Supabase `admin_roles` + `admin_role_permissions` + `admin_assignments` + `has_admin_permission()` + RLS | Mismo | `AdminCutoverRepository`, Roles Claude y RLS | RPC auditadas por `authorization.write`; ningún cliente se promueve | ADR-041 ACTIVE; H005_TEST principal, TEST2/3 normales | No | Rol de sistema inmutable, autoasignación denegada y último principal protegido. Cargo, sindicato, puesto, tags y `numero_control` no conceden permisos. |
| Responsabilidad editorial por sección | RESOLVED — Supabase `admin_section_definitions` + `admin_section_responsibilities` + `admin_section_responsibility_actions` + auditoría | Mismo | `get_admin_access_context`, `AdminRepository` y enforcement RLS/trigger de cada sección | H005_TEST mediante RPC auditadas por `authorization.write`; nunca el propio responsable | ADR-046/047 — 11 secciones `ENFORCED`, cero `DESIGN_ONLY` | No | Email sólo resuelve una cuenta confirmada; la autoridad durable es UUID. Acciones independientes, RLS/trigger `OLD/NEW`, paths por sección+UUID, revocación backend y origen histórico protegido; Minutas es frontera propia. |
| Ahorro | RESOLVED — Google Sheets + Apps Script | Mismo hasta equivalencia/cutover expresamente autorizado | `financial-legacy` backend → `FinancialLegacyRepository`; browser recibe solo su proyección | Sistemas legacy autorizados | Phase 7 OPTION A / ADAPTER READ-ONLY | Sí | `DATA`, mocks y browser storage fueron retirados del camino financiero. Error legacy produce estado controlado. |
| Simulador ilustrativo Suti Inversión | El contrato presentacional aprobado por el propietario fija exclusivamente `2.5%` mensual, monto `$50,000–$2,000,000`, paso `$10,000` y plazos `6/12/18/24`; no es autoridad operativa | Mismo mientras no exista una H de producto con backend autorizado | `InvestmentScreen` deriva en memoria rendimiento mensual y total simple; no lee saldo, elegibilidad, contrato, solicitud ni producto vigente | Ninguno; no persiste y el CTA no crea una inversión | H-SUTI-INVERSION-SCREEN-001 PRESENTATION ONLY | Sí — inversión operativa permanece legacy | Estado efímero del componente, sin `localStorage`, mock, fallback, Google, Supabase, Edge o RPC. Borrar/refrescar reinicia el default. Los resultados son ilustrativos y no autorizan pagos, depósitos, rendimientos ni altas. |
| Préstamos | RESOLVED — Supabase conserva perfil, programas, fondos, reglas, visibilidad y política de plazo | Mismo | `financial-legacy` genera `financial_session_snapshots` personalizados TTL 15m; `resolve_current_loan_snapshot_quote` resuelve monto/fondo/plazo con el motor certificado | Admin financiero mediante RPC auditadas; Edge service-only genera snapshot y confirma; RPC autenticada sólo lee internamente el snapshot propio | ADR-042 + ADR-043 + ADR-064 + ADR-065 ACTIVE | Google sólo conserva histórico/procedencia | Snapshot y RPC no son autoridad. Apertura, interacción y confirmación usan 0 lecturas Google; TTL/perfil/contexto/política/versión de criterios invalidan. Confirmación recalcula contra el batch Supabase activo antes del alta atómica. |
| Nómina quincenal declarada para simulación | RESOLVED — Supabase `affiliate_payroll_declarations` | Mismo | `PayrollDeclarationRepository`; RPC `get_current_declared_payroll`; `financial-legacy` adjunta la proyección informativa mediante `get_current_declared_payroll_impact` | Afiliado Auth propio o administrador activo bajo sesión contextual válida mediante `save_current_declared_payroll` | ADR-050 + ADR-051 ACTIVE | No sustituye nómina oficial ni Google financiero | Percepciones/deducciones son declaradas, no extraídas de un talón. RLS forzada, versión, auditoría de actor/contexto/sesión/motivo. El 30% nunca decide elegibilidad o aprobación. |
| Solicitudes de financiamiento | Supabase `program_requests` conserva solicitud y snapshots inmutables de envío/aprobación; Google `Historial de solicitudes` recibe una proyección append-only después de aprobación | Mismo | Afiliado/Admin desde Supabase; legacy lee la fila confirmada | Edge `loanSessionConfirm` revalida perfil/política/criterios Supabase y llama RPC service-only atómica; Edge Admin + Apps Script sólo para append aprobado | ATOMIC CONFIRMATION ACTIVE / WRITER DEPLOYED | Sí | Browser no crea préstamo en pasos separados. `CONDITIONS_CHANGED` no inserta. `financial_submission_snapshot` reconstruye la solicitud sin depender del caché temporal. |
| Solicitudes iniciales de programas, productos y cotizaciones | RESOLVED — Supabase `program_requests` para altas posteriores al corte; `marketplace_quote_requests`, `marketplace_benefit_requests` y `program_benefit_requests` conservan el histórico previo sin nuevas altas | `program_requests` | Afiliado solicitante; empresa destino cuando exista; Admin con `program_requests.read` | RPC autenticada deriva afiliado/control; empresa/Admin actualizan bajo permiso | ADR-038 ACTIVE | Sí solo en procesamiento posterior | La solicitud inicial no escribe Google. Firma, términos, actor real, afiliado, `numero_control`, destino, status e idempotencia quedan en backend. `legacy_reference` solo puede aparecer tras handoff futuro explícito. |
| Registro técnico de export financiero | `program_requests` + `financial_request_export_audit` conservan estado/auditoría; `SutiApp Financial Handoff` reserva UUID/hash/fila para recuperación | Mismo mientras el handoff Google posterior conserve su autoridad de procesamiento legacy | Edge Admin server-to-server y Apps Script | RPC service-only + registry idempotente por `program_request_id` bajo `LockService` | ADR-044 DEPLOYED | Sí | No recibe altas de usuario ni criterios financieros. Una reserva `processing` permite recuperar timeout antes/después del append; `processed` solo se marca tras leer y verificar la fila. |
| Exportaciones operativas XLSX/CSV | Cada tabla Supabase productiva conserva autoridad; el archivo es una proyección temporal y `data_export_audit_log` sólo registra metadatos | Mismo | `DataExportRepository` → Edge `data-exports`; Admin con permiso global o acción `export` de sección | Ninguno sobre datos maestros; Edge inserta únicamente auditoría de exportación | ADR-048 ACTIVE / LIVE MATRIX PASS | Sí sólo para la proyección de `program_requests`; Google no se consulta | 17 perfiles allowlisted, máximo 20,000 filas, XLSX/CSV `no-store`, 11 secciones con `export` disponible y cero grants automáticos. Auth/secrets/firmas/binarios/Storage internals excluidos. |
| Catálogo financiero | Supabase `financial_programs` + `financial_funds` + `financial_rules`; `finance_catalog_presentation` conserva sólo presentación y `loan_term_policy` los plazos UI | Mismo | Edge/RPC service-only, Admin autorizado y snapshot temporal personalizado | RPC financieras auditadas con permisos `financial_programs.*`, `financial_rules.*`, `financial_rates.write`; ningún writer Google | ADR-065 ACTIVE / CUTOVER PASS | Google sólo procedencia | DRAFT/PUBLISHED/SCHEDULED/EXPIRED, versión, motivo y auditoría. El snapshot personalizado TTL 15m sigue siendo derivado y nunca fallback ni segunda autoridad. |
| Catálogo marketplace | RESOLVED — Supabase `marketplace_categories`, `marketplace_products`, `marketplace_product_assets`, `marketplace_promotions` + Storage | Mismo | Marketplace, Convenios, detalle, Admin y Panel Empresarial | H005_TEST por permisos RLS; miembros de empresa solo sobre su tenant | MASTER Phase 3 ACTIVE — 3 categorías históricas, 0 productos inventados | No | `catalogStore`, `DATA` y `localStorage` dejaron de ser autoridad. Categoría/subcategoría no reconciliada se conserva como `category_raw`/`subcategory_raw` TEXT. |
| Convenios administrativos / productos empresariales | RESOLVED — Supabase `companies`, `company_benefit_profiles`, `company_benefits`, `company_audience_rules`, `banners` y tablas `marketplace_*` | Mismo | Convenios, Marketplace, Admin y Panel Empresarial | Admin autorizado; miembros empresariales según policies de dominio | ADR-041 PRODUCTIVE_SUPABASE | No | UI Claude completa; segmentación usa códigos canónicos; sin store, DATA o localStorage autoritativo. |
| Noticias y artículos | RESOLVED — Supabase `public.news_articles` + `news_settings` + `app_assets`/Storage | Mismo | `NewsRepository`; Inicio/artículo; Admin Noticias | Principal técnico con `news.write` o responsable UUID con acción exacta `read/create/update/delete/publish/order/assets`, validada por RLS/trigger | PHASE 2 ACTIVE / ADR-046 NEWS OWNERSHIP ENFORCED | No | `DATA.noticias` y `adminStore` no son fallback. Assets de responsables se aíslan bajo `news/<auth.uid()>/`; `record_origin` es inmutable y el borrado histórico permanece denegado. |
| Educación y tutoriales | RESOLVED — Supabase `public.educational_resources` + `app_assets` + Storage | Mismo | `EducationalRepository`; Admin Educación; pantalla pública futura conserva estado pendiente | Importador histórico autorizado; H005_TEST con `content.write` por RLS | PHASE 2 ACTIVE — 32 históricos, 0 publicados | Sí solo por tema en 4 tutoriales de Ahorro | Google es procedencia read-only: 28 Educación + 4 Tutoriales, snapshot `B3E940D6508FA0FC571E16097391B69D859AD8F1BA587E536F5A2D312314E1E4`; no se migró lógica financiera. |
| Copy explícitamente administrable | DORMANT — Supabase `public.managed_copy_overrides` se conserva sólo como histórico recuperable | Ninguno en runtime | Ninguno; `ManagedCopyRepository`, `copyStore` y `LiveText` fueron retirados | Ninguno desde frontend; la tabla y sus filas no se borran | UI RETIRED por instrucción del propietario 2026-08-27 | No | La aplicación vuelve a presentar el copy estructural versionado en código. No existe botón, modo de edición en vivo, panel de permisos ni writer frontend para overrides. |
| Banners Home | RESOLVED — Supabase `public.banners` + `public.app_assets` + Storage | Mismo | `BannerRepository`; Home; `VisualCrudModule` Admin | Importador H-007.2 y admin con `banners.write` mediante RLS | H-009 CRUD ACTIVE — 10 activos | No | Filas históricas conservan procedencia; nuevas filas usan `record_origin=ADMIN_H009`. Sin fallback hardcodeado/Glide/local. |
| Popups productivos | RESOLVED — Supabase `public.popups` + `public.app_assets` + Storage; 3 históricos preservados y 0 activos | Mismo | `PopupRepository`; shell; `VisualCrudModule` Admin | Importador H-007.2 y admin con `popups.write` mediante RLS | H-009 CRUD ACTIVE / 0 ACTIVE | No | Los tres candidatos históricos permanecen deshabilitados; H-009 permite que el administrador cree contenido explícito sin inventar semántica para esas filas. |
| Propuestas empresariales de pop-ups | RESOLVED — Supabase `public.company_popup_proposals` | Mismo | Empresa autora por tenant; Admin Aprobaciones con `popups.read` | Empresa miembro con plan activo que permita pop-ups; Admin mediante `review_company_popup_proposal()` y `popups.write` | H-ADMIN-REMAINING ACTIVE | No | Una aprobación crea un borrador deshabilitado en `popups`; publicar sigue siendo una acción administrativa separada. No existe autoridad local ni aprobación solo UI. |
| Empresas públicas / directorio de convenios | RESOLVED — Supabase `public.companies` + `public.company_assets` + Storage, inicializada desde `Convenios2` | Mismo | `CompaniesRepository`; Convenios/detalle; `VisualCrudModule` Admin | Importador H-007.3 y admin con `companies.write` mediante RLS | H-009 CRUD ACTIVE — 33 empresas / 35 vínculos | No | Histórico raw intacto; nuevas empresas administrativas no fingen hoja/ordinal/hash. Desactivadas permanecen en autoridad pero no aparecen al público. Marketplace, productos, planes y Auth empresarial siguen fuera. |
| Acceso empresarial Marketplace | RESOLVED — Supabase Auth + `marketplace_company_memberships` | Mismo | Panel Empresarial y RLS tenant | Proceso administrativo con `company_portal.write`; el cliente no se autoasigna | MASTER Phase 6 ACTIVE — 0 membresías productivas inventadas | No | El password empresarial simulado quedó deshabilitado. Una membresía técnica no inventa un plan ni una suscripción. |
| Planes y suscripciones del Portal Empresarial | RESOLVED — Supabase `company_portal_plans` + `company_portal_subscriptions` | Mismo | Admin autorizado; empresa miembro por RLS tenant | H005_TEST con `company_portal.write`; ningún cliente normal o empresa se autoasigna | MASTER Phase 6 ACTIVE — 0 planes / 0 suscripciones | No | Catálogo y suscripciones iniciaron vacíos. Ausencia de términos aprobados se proyecta como `pending`; no existe fallback a `DATA`, mock, JSON, caché o almacenamiento del navegador. |
| Catálogo de membresías | RESOLVED — Supabase `membership_offerings` + `app_assets`/Storage | Mismo | Finanzas/Membresías y Admin | H005_TEST con `memberships.write` por RLS | MASTER Phase 4 ACTIVE — 6/6 históricos | Sí en solicitudes/descuento nómina, no en catálogo | `membershipStore` es solo estado en memoria; SEED/localStorage/URLs externas dejaron de ser autoridad. `Solicitudes membresía` permanece Google legacy con PII/documentos y no se copió. |
| Catálogos maestros por programa | RESOLVED por subdominio autorizado — Supabase `program_catalog_items` + `program_catalog_item_assets` + Storage | Mismo | `ProgramCatalogRepository`; Finanzas, programa, cards y detalle Claude | Importador H-DATA-CUTOVER-001; solicitudes iniciales por `program_requests` | ACTIVE — 134/134 filas + 268/268 asset links | Sí: cálculos y procesamiento financiero posterior permanecen legacy | Incluye Farma 50, Tours 17, Auto 3, Renta 1, Casa 35, Solar 4, Aires 16, Puertas 3, Cómputo 1, Donativos 1 y Terrenos 3. Todos los renglones habilitados pueden registrar intención sin tocar finanzas. `source_payload` no se concede al browser. |
| Assets visuales/documentos migrados | RESOLVED — `public.affiliate_files` + `public.private_assets`/`public.app_assets` + Supabase Storage | Mismo | `AffiliateRepository.getDocuments()`; afiliado efectivo/admin `assets.read` bajo RLS; pantalla Documentos read-only | Importadores/sincronizadores y writers administrativos autorizados; pantalla no escribe | PHASE 2 PASS/CLOSED; PHASE 3 E2E PASS | Según dominio | `source_url` es solo procedencia. 12,901 relaciones enlazan UUID; expediente runtime sin `DATA`/localStorage/fallback. Privados usan URL firmada de 300 s. Cinco huérfanos `app-assets` quedan fuera bajo `APP-ASSETS-ORPHAN-AUDIT`; `private-assets` huérfanos 0. |
| Assets visuales no migrados | SOURCE OF TRUTH CONFLICT | UNRESOLVED | Marketplace, noticias y paneles no migrados | `image-slot`, `.image-slots.state.json`, stores/browser | Prototipo | Según dominio | Permanecen fuera del camino runtime H-007/H-007.2/H-007.3; requieren H específica. |
| Branding, ícono e instalación | RESOLVED — Supabase `public.app_settings` + `public.app_assets` + Storage; `home.header.collapsed` usa `app_assets.asset_key` directo | Mismo; archivos PWA y default versionado del header son derivados reproducibles | `BrandingRepository`, `AssetRepository`, `VisualContent`, `assetsStore` en memoria y `AssetsResolver`; Admin, Home, sello runtime, HTML/manifest/service worker derivados | Admin con `assets.write` mediante RLS; sincronizador server-side autorizado para copias estáticas | H-008 + H-HOME-HEADER-001 ACTIVE | No | `home.header.collapsed` aplica la precedencia explícita ADR-054: override Admin → slot de usuario → default local versionado → icono. La proyección Supabase es descartable y notifica sin polling; `localStorage` no puede sobreescribirla. Los demás recursos conservan INV-027. |
| Historial/seguimiento de solicitudes iniciales | RESOLVED — `program_requests` + su `workflow_snapshot` inmutable; catálogo versionado en `operational_workflows`/`operational_workflow_stages` | Mismo | Éxito, Mi Historial, Tracking Claude y Admin | El alta congela el flujo efectivo; `resolve_program_request_workflow_state()` proyecta la misma línea de tiempo y `list_self_program_request_history()` la entrega sólo al afiliado efectivo | ADR-038 + ADR-079 + ADR-080 ACTIVE | No para la intención; sí para fases financieras posteriores | `DATA.solicitudes`, arrays de etapas, `financeStore`, `flowStore/localStorage` y Google no son autoridad. Cambiar Admin afecta solicitudes nuevas; una solicitud existente conserva versión, textos, orden, responsable y SLA originales. |
| Directorio / Comité | RESOLVED — Supabase `public.directory_members` + `app_assets`/Storage | Mismo | `DirectoryRepository`; Inicio, módulo Comité y CRUD Admin canónico | Importadores H-007/H-007.2; CRUD bajo frontera `documents` y RLS de `20260823000800` | SUPABASE_ACTIVE 30/30; CRUD CUTOVER ACTIVE | No | Histórico protegido por `record_origin`; filas Admin usan `ADMIN_SECTION_ROLLOUT`. Miembros sin foto muestran placeholder; sin `DATA.comite`, Google o almacenamiento local. |
| Minutas | RESOLVED — Supabase `public.minutes` + Storage | Mismo | `MinutesRepository`; módulo Minuta | Importadores H-007/H-007.2 y principal/responsable UUID con acciones exactas mediante backend/RLS | SUPABASE_ACTIVE + OWNERSHIP_ENFORCED — 5/5 metadata + 5/5 archivos | No | URL histórica queda solo en procedencia; runtime usa Storage. Origen histórico protegido y `assets` separado. |
| Descargas / Normas | RESOLVED — Supabase `public.institutional_documents` + Storage | Mismo | `InstitutionalDocumentsRepository`; Normas/Formatos; `VisualCrudModule` Admin | Importadores H-007/H-007.2 y admin con `documents.write` mediante RLS | H-009 CRUD ACTIVE — 8/8 metadata + 8/8 archivos | No | Filas desactivadas se ocultan al público sin borrarse. Runtime usa Storage; URLs históricas son procedencia y nunca fallback. |
| Secretaría de Finanzas informativa | RESOLVED — Supabase `public.institutional_programs` + assets Storage | Mismo | `InstitutionalProgramsRepository`; módulo Finanzas | Importadores H-007/H-007.2 y principal/responsable UUID de `programs` con acciones exactas mediante backend/RLS | SUPABASE_ACTIVE + OWNERSHIP_ENFORCED — 17/17 + 17 imágenes | Sí, frontera financiera | Solo contenido visual/informativo. `T:V`, solicitudes y toda lógica financiera siguen excluidos y sin interacción. |
| Catálogos de segmentación de afiliados | RESOLVED — Supabase `segmentation_catalog_entries` + `affiliate_segment_tags`; snapshot Google es procedencia read-only | Mismo | audiencias de pantallas, Convenios y Sindicato | Admin con `segmentation.write` bajo RLS | ADR-041 ACTIVE — 20 valores exactos | No para segmentación; procesamiento financiero posterior al alta puede seguir legacy | Sindicatos, categorías laborales, género y tags nunca equivalen a roles técnicos ni conceden administración. Los criterios de elegibilidad de ADR-065 sí son Supabase. |
| Configuración App/Contenido catalogal | RESOLVED por frontera — estructura/rutas/formularios/copy estructural en código; políticas dinámicas en `screen_access_policies`; `managed_copy_overrides` permanece histórico e inactivo | Mismo salvo la tabla histórica inactiva | shell, navegación y módulos Claude | cambios estructurales por versión; visibilidad por Admin/RLS; sin writer de copy global | ADR-041 PRODUCTIVE_HYBRID / LIVE COPY RETIRED | No; formularios financieros siguen Phase 7 | No existe CMS genérico ni writer estructural en browser storage. |
| Etapas y seguimiento de solicitudes | RESOLVED — Supabase `operational_workflows`, `operational_workflow_stages`, `operational_request_tracking`; snapshot versionado dentro de `program_requests` | Mismo; la definición vigente gobierna sólo altas futuras y el snapshot gobierna cada solicitud ya creada | Admin → Finanzas → Etapas y seguimiento; Éxito, Historial y detalles Admin | Admin con `workflow.read/write`; trigger de alta congela exactamente un flujo y el resolver central combina snapshot + tracking | ADR-080 ACTIVE / LIVE CERTIFIED | Sí cuando una etapa depende de resultado financiero | No calcula ni sustituye resultados Google. Retiro lógico y reordenamiento RPC; no hay delete físico, etapa hardcodeada, mock, `DATA`, localStorage ni fallback productivo. |
| Tres pantallas canónicas restantes de Tu Sindicato | RESOLVED — Supabase `union_screen_content` + `union_content_blocks` + `app_assets`/Storage | Mismo | módulos Categoría, Antigüedad y Jubilados | Admin con `union_content.write`; upload por paths `sindicato/<auth.uid()>` bajo `20260823000800` | CANONICAL CUTOVER ACTIVE | No | Cabecera y bloques conservan solo relaciones UUID. Emergencias no tiene ruta frontend ni consumidor productivo y queda `OBSOLETE`, sin borrar sus filas históricas. |
| Catálogos Marketplace | RESOLVED — Supabase `marketplace_categories`; Google `Categorías SutiCompras` es procedencia read-only | Mismo | Marketplace, Admin y Panel Empresarial | Admin autorizado por RLS | MASTER Phase 3 ACTIVE — solo 3 categorías comerciales semánticamente claras | No | No se migró `Choice` ni los 27 catálogos en bloque. Programas y cualquier categoría financiera permanecen separados. Snapshot `ADF179D4A28F28798187AFF4D0E046E84AD798739FCE896C71AD248E3CAB6FF7`. |
| Catálogos financieros legacy excluidos de ADR-065 | Google legacy | Google legacy hasta auditoría/equivalencia autorizada | Flujos históricos de inversión, pago/cobro, rifa y plazos | Sistemas legacy; writer exacto UNKNOWN | H-007.1 CLASSIFIED / NOT MIGRATED | Sí | No incluye `Criterios de fondos`, ya cortado a Supabase. Inversión, `pendiente de pago`, `SIN COBRO`, formas/plazos/estados de rifa y plazos de pago permanecen fuera de Supabase. |
| Helpers Glide catalogales | No son autoridad de negocio | Retiro futuro tras inventario Glide | Glide | Glide/UNKNOWN | H-007.1 CLASSIFIED / NOT MIGRATED | No | Row IDs huérfanos no se convierten en entidades ni UUIDs de negocio. |
| Notificaciones y promociones | `window.DATA` mock | UNRESOLVED | Pantallas correspondientes | Código fuente | Prototipo | No determinado | Fuera de H-007. |

Cambiar cualquier fila requiere ADR, lectores/escritores enumerados, plan de recuperación y post-auditoría.

## Descomposición H-001 — Usuarios

| Dimensión | Estado |
|---|---|
| Fuente actual en frontend | `SOURCE OF TRUTH CONFLICT`: `DATA.user`, viewer, snapshots de finance/quotes/company, foto y banco locales |
| Fuente histórica externa | `RESOLVED`: `Usuarios SUTIAPP.xlsx` / `Usuarios`; 947 registros; orden físico y hash preservados como procedencia H-002/H-004 |
| Fuente productiva vigente | `public.affiliates`: 947 registros reconciliados; UUID interno y vínculo Auth nullable; `numero_control` TEXT raw, nullable y no unique |
| Credenciales | APROBADO conceptualmente: Supabase Auth; email histórico raw, normalizado derivado y email Auth separados |
| Login productivo | IMPLEMENTADO H-005/H-006: email/contraseña, sesión persistente, restauración, logout, resolución por `auth.uid()` y una proyección única en memoria para TopBar/Inicio/Perfil/Credencial; tres cuentas de prueba controladas, sin activación masiva |
| Impersonación | IMPLEMENTADA Phase 1: backend/RLS, permiso explícito, motivo, actor/contexto separados, auditoría, cierre y TTL máximo de 30 minutos; no anidable |

## Corte 2026-08-25 — expediente canónico, solicitudes, banco, términos y QR

| Dominio | Autoridad única | Contrato |
|---|---|---|
| Catálogo documental | Supabase `document_types` | 13 tipos; los ocho nombres históricos iniciales son estables. Admin autorizado administra presentación, capacidades de cámara/archivo, activación y orden mediante RPC auditada. |
| Expediente electrónico | Supabase `affiliate_documents` relacionado con `affiliate_files`/`private_assets` y objeto privado existente | El backfill relaciona 3,421 instancias con archivos existentes. La versión más reciente por tipo gobierna el flujo; un `VERIFIED` no se muta y su reemplazo crea otra fila enlazada. Una URL firmada temporal no es autoridad. |
| Requisitos documentales | Supabase `program_document_requirements` | Configuración central por programa, empresa, producto, servicio o membresía; producto hereda empresa y admite adición/exclusión/restauración. `program_requests.document_requirements_snapshot` congela configuración y `request_documents` fija los archivos usados. |
| Datos bancarios | Supabase `affiliate_bank_accounts` es la única autoridad productiva; `Usuarios SUTIAPP.xlsx` fue exclusivamente seed histórico | Seed aplicado: 504 cuentas parciales seguras por `numero_control` exacto/único, 8 ambiguas y 1 irrecuperable omitidas. Excel no participa en runtime. Afiliado mantiene 0..N cuentas mediante RPC/RLS; Admin sólo lee con `bank_accounts.read`. |
| Términos | Supabase `program_terms_versions` y aceptación inmutable por solicitud | La autoridad quedó resuelta por el propietario: texto o PDF, versión, publicación, vigencia e historial por programa. Sólo una versión publicada y vigente permite enviar. El propietario aporta el contenido; no existe fallback legal. La ampliación de vigencia/PDF/aceptación se implementará después del dry run bancario. |
| QR de credencial | Supabase `credential_qr_settings` + `credential_qr_tokens` | Política global allowlisted, token aleatorio efímero cuyo hash se persiste; el QR local codifica ruta y token, nunca PII. |
| Solicitudes de membresía | Supabase `program_requests` + `membership_offering_id` | Alta RPC con actor, afiliado, perfil, términos y documentos; historial/Admin consumen la misma fila. |

Google de Ahorro/Préstamos, Apps Script, tasas, fórmulas, saldos, amortización y conciliación permanecen sin cambios. La ausencia de términos aprobados falla cerrada y requiere decisión del propietario, no una segunda autoridad.

## Corte 2026-08-25 — higiene del expediente histórico

`affiliate_documents` + `document_types` continúan siendo la autoridad del expediente vigente. `affiliate_files.expediente_classification` es una proyección semántica mantenida por trigger para aplicar el corte de lectura sobre relación, `private_assets` y Storage: afiliado sólo `CURRENT_DOCUMENT`; Admin con `assets.read` conserva auditoría histórica. No existe copia, fallback ni autoridad paralela.

Clasificación live: 3,421 vigentes; 901 versiones históricas; 5 legacy; 7 sin clasificación documental concluyente; 8,567 técnicos `HISTORICAL_NON_DOCUMENT`. Se preservaron 12,901 relaciones, 25,358 registros de procedencia, 13,048 activos privados y 13,051 objetos privados. La auditoría de borrado es una fase separada y su resultado actual es cero eliminaciones.

## Corte 2026-08-29 — disponibilidad física y reemplazo documental

`affiliate_documents` + `document_types` siguen siendo la autoridad canónica; `private_assets` y la existencia del objeto en `private-assets` completan la disponibilidad física. `list_effective_affiliate_documents` deriva el afiliado efectivo sin aceptar objetivo y `list_admin_affiliate_documents` exige `documents.read` más objetivo explícito. Ambas proyecciones omiten rutas y URLs. `DocumentWorkflowRepository.selfPreview/adminPreview` invoca `document-access`, que autoriza un solo documento, firma por 300 segundos y registra la firma antes de devolverla.

El afiliado puede cargar una nueva versión de un tipo ya verificado. `register_affiliate_document` conserva intacta la fila `VERIFIED`, crea otra `PENDING_REVIEW`, enlaza `replaces_document_id` y audita `REPLACEMENT_UPLOAD`. La versión más reciente decide si el requisito está cubierto; no se permite volver silenciosamente a una versión anterior cuando la nueva fue rechazada o perdió su objeto físico. El trigger de `request_documents` repite esta validación en backend y aborta atómicamente la solicitud cuando falta un objeto.

## Corte 2026-08-30 — aislamiento de contexto documental

Autoservicio y Administración son contratos separados. El primero sólo puede leer y previsualizar el expediente del afiliado efectivo derivado desde Auth/impersonación; un Admin dentro de autoservicio no hereda acceso global. El segundo conserva la capacidad `documents.read`, pero requiere afiliado objetivo explícito en cada listado y vista. `document_access_audit_log` es bitácora append-only de contexto y propósito, no autoridad documental ni caché; nunca almacena URL, token o ruta. Los `VERIFIED` importados conservan su estado histórico y se proyectan como `Histórico importado` cuando no existe evidencia de revisión humana, sin reclasificación masiva.
