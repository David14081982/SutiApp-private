# H-SAVINGS-ACCESS-001

PRE-CHANGE AUDIT — PASS for implementation and isolated tests.
Owner: administrator must add people by email; leave open now, retain restricted-list option. Interpretation explicitly communicated: open means existing authorized administrative access, never public/ordinary-user financial access.
Domain/authority: reuse admin_section_definitions/responsibilities and confirmed Auth UUID email resolver. Add only Savings access mode (OPEN default / RESTRICTED). No parallel membership list. Email is lookup input, never savings identity; Folio unaffected.
Scope: migration/recovery 20260906000700_savings_admin_access; app/savings-access-admin.jsx, savings-review-repository.js, screens-admin-savings.jsx, build-bundle.js and generated artifacts; focal access SQL/browser tests and two dependent Admin browser fixtures; audit/QA/guide, authority/changelog and derived registry. Isolated delivery based on published 5e60f92 excludes pending operations 001–004 and unrelated Auth edits.
Backend: preserve existing function OIDs/grants while wrapping has_admin_permission and get_admin_access_context; private original-definition copies, not rename-in-place (existing policy dependencies must remain valid). Savings-only gate; all other permission results unchanged. Existing permission managers retain access. Restricted ordinary admins require section read assignment; added people gain only savings.read and private review edit capability. Do not grant savings.write/approve/config/identity_review to new delegates: those can affect operational financial functions. Private review functions use explicit delegated edit helper. No existing role/assignment is replaced.
UI: add Acceso a Ahorro above review. Mode, add confirmed email, consult/review level, listed people, revoke and audit. No change to user Savings/Inicio/Finanzas. Public account registration remains the existing Auth process; unconfirmed/missing email gets explicit error; no invitation emails or new Auth principals are sent/created.
Security: force RLS and private backups/mode/audit; only authorization.write manages mode/list. Reuse audited set/revoke_section_responsibilities. No self-elevation, fuzzy email/person match, broad-role replacement, direct table DML, fallback or stored secrets. Backend revocation applies on next RPC even if UI session is stale.
Verification: OPEN keeps existing admin behavior and excludes ordinary/anonymous users; scoped delegate can review but cannot call operational writers or other domains; RESTRICTED blocks unlisted savings-role administrator; managers cannot lock themselves out; revoke immediate; optional observations preserved; recovery before use and refusal after configuration/membership history. Compare full original access context for existing admins when OPEN. Existing global image matrix is required for global backend permission/context changes; current automated credentials are invalid and must not be called PASS.
Recovery: originals and definition metadata privately backed up; restore only with zero later mode/list activity and no changed reviewed definitions. Never delete membership/audit history to permit recovery. No financial rows, spreadsheets, formulas or Auth assignments changed.
UI preservation: retain published 18 tabs (in-progress workspace 19); add a bounded access control card; same sidebar/routing. Core Auth frontend unchanged.
