# H-SAVINGS-ADMIN-REVIEW-001 — stages 1–4

PRE-CHANGE AUDIT
Status: PASS for implementation and isolated verification; deploy requires verified backend and frontend.
Owner authority: complete stages 1–4; administrative review before publication; optional observations for reconciliation. Existing authorization does not certify balances or authorize external sheet edits.
Objective: usable private per-source-row review, all imported rows retained, editable candidate fields, immutable revisions, explicit review status, role-gated access and no effect on public balances.
Scope/files: this audit; independent migration/recovery 20260906000500_savings_admin_review; app/savings-review-admin.jsx; app/savings-review-repository.js; focal integration in screens-admin-savings.jsx; build-bundle.js/generated bundle/SutiApp.html/sw cache; import and SQL/browser test scripts named savings-admin-review; operating guide and QA evidence; SOURCE_OF_TRUTH, AGENT_CHANGELOG and derived registry. Delivery may use an isolated checkout of current HEAD with only review integration, existing Ahorro navigation naming/grouping, and generated artifacts; never include unrelated pending Auth work. If shared navigation is delivered, global regression is required.
Tables: new savings_review_batches, savings_review_records, savings_review_events only. No changes to canonical ledger, participants, affiliate links, self readers, source evidence, formulas, loan records or Auth assignments.
Authority: Google Q remains historical payout reference; private snapshot is timestamped import evidence, not current payment authority. Supabase review records are authority only for administrative review proposals. No publication endpoint or source connection from self readers exists.
Readers: permission-gated review RPC and dedicated Admin component. Writers: controlled backend import and revision RPC. No browser DML, fallback, local storage, automatic certification, recalculation, fuzzy linking or deletion.
Identity: exact TEXT Folio, preserve zeros; live padron label first source ordinal + DUPLICADO or SIN REGISTRO. Corrections to source Folio require identity-review permission and never change padron or existing relations. Pending identity cannot be marked resolved unless exact unique match exists. Loan identity conflicts stay reviewable without editing Loans.
Review model: all five Savings sheet rows plus identified loan incidents; source fields immutable; proposed values separately versioned, optional observations, actor/context/time, optimistic version + actor/command idempotency. Resolving a row means administrative review only, never certified ledger or publication. All source fields remain visible; derived financial fields require explicit review, not fabricated formulas.
Risks: concurrency/lost updates, cross-user access, ambiguous identity, omitted source rows, mixed-date snapshots, altered previous reader, unrelated staged deployment. Mitigation: immutable source and events, constrained RPC, complete counts/hash, isolated tests, source reader hashes and financial counts before/after, minimal independent backend.
Recovery: no existing objects replaced; removal allowed only before imports/reviews; after use disable delivery/access via reviewed backend change and preserve tables/history; private full backup before live import. No destructive rollback after use.
Tests: SQL transaction fixtures + recovery; anonymous and ordinary role denial; review write without notes; null vs zero; stale updates/idempotency; unchanged self reader/evidence/ledger; browser detail/filter/preview/save/retry/read-only/responsive; build and exact scope; real access compatibility. No financial promotion.
Legacy: READ ONLY; use existing timestamped private capture. Capture date must be shown and no claim of live bank/loan validation made.
UI contract: preserve existing Admin sections, navigation and self UI. Add Conciliación y revisión as first/default section, with its own full-width queue/detail and explicit unpublished status. Existing sections are retained. Operational stages 5–7 remain outside this H.

Scope expansion before dependent test updates: test-savings-operations-browser.js and test-savings-identity-guard-browser.js load the added review component with isolated review fixtures, select the pre-existing sections explicitly, and verify 19 tabs (18 retained + review).

Delivery scope detail: savings-admin-review-db.js provides local management transport without dependency on the pending operational test harness; audit-savings-cutover-readiness.js is a read-only import parser dependency. Deployment checkout normalizes vendored files to exact Git blob bytes to preserve SRI during Windows local validation; no vendor source change. The independent delivery contains 18 tabs (17 published tabs retained + review); the main in-progress workspace contains 19 (also pending operations tab).

Prepublication normalization fix: independent source inspection detected blank monetary inputs normalized to zero and a zero-based/one-based matrix-boundary mismatch (Z/DO). The immutable raw capture is correct; no review events or public delivery exists. Expand scope to migration/recovery 006 review_import_normalization and scripts/repair-savings-admin-review-import.js plus independent import tests. Preserve the full pre-fix derived source_data/field_defs in a private RLS backup table and existing external backup. The one-time guarded correction may change only these derived projections, under table locks, with zero review versions/events, verified raw equivalence and exact row IDs. Raw source, Folio, source coordinates, proposals, public evidence and financial data remain untouched. Never replay after user review; recovery must preserve backups/history. This fixes our import interpretation, not historical source data.
