# H-SAVINGS-CERTIFICATION-001

## AUDIT / AUTHORITY / PLAN / RISK

Owner authorizes continuing definitive balances and future projections (2026-09-07). This does not certify any individual account or authorize publication of unreviewed amounts. Architecture lookup is stale; directed inspection confirmed the deployed four-tab panel, private review RPCs, zero enrollments/plans/transactions/certified participants, and unapplied operational migrations 001–004.

Scope: implement explicit per-Folio opening certification, server preview, immutable component evidence, confirmed receipts, and dated projections in the existing person detail. Ordinary observations remain optional. No automatic certification, historical replay, Google writes, credentials changes, or member publication. Previously staged operational migrations are dependencies, reviewed/tested before application; no imported record becomes an actual receipt through elapsed time.

Files: this audit; new migration/recovery 20260907000100_savings_balance_certification; app/savings-certification-admin.jsx; app/savings-panel-admin.jsx; app/savings-panel-repository.js; scripts/build-bundle.js; focal certification SQL/browser tests and evidence; scoped application script; docs/SOURCE_OF_TRUTH.md, INVARIANTS.md, DECISIONS.md, AGENT_CHANGELOG.md and architecture generated index. Bundle and delivery cache/version artifacts only after verification. Existing staged operations 001–004 and components may receive focal compatibility fixes, recorded here before editing. Unrelated dirty files remain untouched.

Authority: immutable savings_review_records and batches retain source Q and corrections. An officer-approved opening becomes canonical CAPITAL/YIELD REGULARIZATION entries in savings_transactions, with savings_balance_certifications recording source version, source fingerprint, explicit component confirmation and cutoff. Q is a reconciliation reference, never credited together with matrix or historical withdrawals. Future expected amounts derive only from dated savings_contribution_plans; received amounts require audited confirmation. Google/member read authority remains unchanged until a separate publication decision.

Identity: exact original Folio must match one active affiliate and one participant; ambiguous, missing, proposed reassignment, or conflicting prior participant link cannot certify. No email or numeric normalization. Locks cover identity, review version, participant and command retry. No deletion or historical overwrite. No assumption about remaining yield after partial withdrawals: the officer must enter the confirmed remaining capital and yield, whose sum matches the reviewed opening.

Security: read requires savings.read; definitive financial approval requires savings.approve; actual receipt writer requires savings.write. Private review delegation alone does not elevate financial approval. Auth actor and context recorded separately. New tables force RLS and deny direct client writes. Private certified balances must not leak through older self-service RPCs.

Tests: migration and reverse recovery inside ROLLBACK; idempotency/payload conflict, stale source version, duplicate/missing Folio, optional notes, components sum, explicit zero versus missing receipt, future receipt rejection, actual-vs-expected differences, schedule boundaries, no automatic yield, no source edits, no financial changes from preview, unauthorized/anonymous denial; browser person navigation and responsive layout. Inspect real application fingerprints before/after additive schema application.

Recovery: preserve original function definitions; additive recovery only before any certification/receipt. Once financial events exist, retain history and use forward correction; never silently discard money records.

UI contract: keep top summary above Cobranza/Ahorradores/Retiros y cambios/Revisión; preserve search, paging, prev/next, date corrections, full withdrawal list, history, source archive, optional observations and container responsiveness. Add a clearly labelled certification/projection card inside the existing person detail; no parallel route.

Status: PASS for scoped implementation; financial certification and public cutover are not claimed.

## Authorized continuation, 2026-09-13

Owner reconfirms Q as the accepted balance reference and authorizes actual receipts, operational Savings flows, manual period yield, reports and one-click coordinated publication AFTER physical acceptance. Do not ask for the already-provided formulas again. Preserve every current UI structure; integrate controls into existing Savings sections. No changes to other program flows or their loan authority; discover and consume the existing debt decision only. PDFs remain deferred. No automatic public switch in this implementation.

Extended scoped files: new Savings-only migration/recovery 20260913000100_savings_runtime_completion; independent tests test-savings-runtime-live.js and test-savings-certification-browser.js; Savings panel repository/admin and new Savings certification/control components; existing Savings operations/yield/retirement components only for integration; focal savings-repository.js invalidation if necessary. Scoped source capture/equivalence scripts and ignored private evidence may be added. Admin Afiliados, global Auth, app shell, other program repositories and Google writers are OUT OF SCOPE. Pending migration 003 global affiliate deletion trigger must not be applied in this Savings-only delivery: use only its exact-Folio display helper as a separately scoped dependency. Applied 20260907000500 Savings read optimization must be preserved.

Before implementation: capture scoped working-file hashes for preservation; current source Q and formula reads are READ ONLY. Related source corrections must retain all eight existing review events and the reviewed record. Source refresh must not overwrite officer proposals. A single global publication authority must change all self readers together and invalidate the existing server-validated cache. Preview/physical acceptance precedes an explicit administrator click. Source draft SQL requires fixes before use (period cutoff cannot be equated to already-paid yield boundary; first-ever contribution is distinct from current enrollment start; correction after confirmation needs audited adjustment; no bypass through old receipt writers).

Additional bounded implementation files: app/savings-runtime-admin.jsx, scripts/test-savings-runtime-browser.js; publication migration/recovery 20260913000200 and test-savings-publication-live.js; requests migration/recovery 20260913000300 and test-savings-requests-runtime-live.js; source refresh migration/recovery 20260913000400 and scoped source apply/test scripts. These expose only Savings request, report and publication RPCs. All user view layout remains unchanged; functional forms use the existing Savings sheets/cards. Read-only source equivalence has proved 366 Q/G equations and 24,796 numeric future cells; three source records have six updated historical deductions since the imported snapshot. Source refresh must retain original rows plus officer proposals/history and only reconcile those proven exact-Folio cell changes.

Functional Savings-only additions: app/savings-request-form.jsx and focal user-action browser verification; existing screens-savings.jsx receives button handlers and data-driven period labels only, preserving all layout/CSS. No other program or shared-shell edit. Existing beneficiary/self dashboard RPC publication guards are included; financial histories remain accepted snapshots until an audited correction.

Current continuation scope: migration/recovery 20260913000500_savings_account_receipts and test-savings-account-receipts-live.js add native canonical account/list and one receipt writer, including renewal, actual economic date, exact identity and posted-yield protection. Savings self forms expose existing sheets for requests, beneficiaries and upcoming dates; test-savings-self-actions-browser.js is an isolated browser fixture. Source observation input/test are scripts/savings-source-observation-input.js and scripts/test-savings-source-refresh-live.js. No real publication is authorized before owner physical testing. Current implementation remains in progress; previous PASS above describes earlier bounded work only.

Final integration scope extension: 20260913000600_savings_admin_current_summary migration/recovery and matching focal test (server-derived private Admin totals, never a second balance store). Unapplied 20260906000400 period-yield definition and new scripts/test-savings-yield-runtime-live.js correct disabled periods, enrollment as of cutoff, and pending-review protection. Release is assembled in ignored tmp/savings-runtime-20260913/release from published main 450213a, preserving unrelated modules and exact published Savings CSS. Only generated bundle/cache version may change for delivery; no shared-shell behavior changes.

Release tooling scope includes scripts/build-savings-release.js and scripts/savings-runtime-release-db.js. Full build of published HEAD revealed pre-existing source/bundle differences in screens-credencial.jsx, screens-admin-fincat.jsx and screens-admin-program-products.jsx. The focal Savings build preserves those published module bytes exactly; no unrelated behavior is regenerated into this delivery. Whole-build syntax is checked, followed by focal module assembly, syntax and module parity. No Google or other-program writes.

006 scope clarification: current money/state must be coherent across private Admin list, filters, neighbor navigation and detail for certified accounts. Preserve historical source fields and historical last_scheduled keys for the existing source-correction handler. Current derived display does not replace raw Q/archive. Public visibility label follows publication authority; layout/CSS remains approved.

Architecture closure includes Savings-only semantic entries in docs/architecture/architecture-overrides.json and its five derived indexes. Public PRIVATE/PUBLISHED gate and confirmed-balance authority must supersede obsolete shadow-only annotations; no other domain metadata is changed.
