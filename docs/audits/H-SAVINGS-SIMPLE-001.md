# H-SAVINGS-SIMPLE-001 PRE-CHANGE AUDIT

Status: PASS. Owner explicitly authorizes the proposed simplification of the private Savings review expediente, keeping old Glide fields as secondary reference and preserving corrections/history.
Scope: app/savings-review-admin.jsx presentation only; focused review/navigation tests and a new simple-expediente fixture suite; generated bundle, SutiApp.html/sw.js version only; this audit, evidence and changelog. Temporary local delivery helpers allowed. Isolated delivery based on e514f75 excludes all pending financial/Auth work in main.
Authority: existing SavingsReviewRepository list/detail/save and savings_review_records immutable source plus separate proposals. No repository, RPC, schema, data, permissions, calculations, Sheets, scripts or formulas change. No implied certification or public balance publication.
Plan: replace the long default field table with a compact balance/amount/start/status summary; fixed person-level section buttons for summary, dated deductions, withdrawals, amount changes, yields, old reference fields and review/audit. Existing correction editors stay accessible within their relevant section; old auxiliary fields move to explicit secondary consultation. Preserve all source fields, missing values, exact Folio, optional notes, all pending changes across sections, save preview/idempotency, read-only mode, previous/next, modal keyboard containment and list return. Do not invent withdrawal events from aggregate source columns or calculate new balances. Dates/values retain original-versus-proposed distinction.
UI authorization: user explicitly requests removal of irrelevant old fields from the main workflow. User screenshots/current component establish baseline. Existing controls/data move, not disappear; loading/error/empty states preserved. No new component dependency, route, RPC or authority. Registry code/schema hashes were current at start; only prior evidence files were stale. Presentation-only change qualifies for Navigator nonstructural exception; declare resulting hash staleness.
Risk: hidden pending edits could be missed; always expose pending count and keep preview across all sections. Source duplicates must not become double totals; summary uses Q only, source withdrawal columns remain separately labelled. Archive must retain every unmapped field. Recovery: revert scoped UI artifact commit, no database recovery required.
Verification: real-browser isolated 390/1440 checks of compact default, section visibility, auxiliary fields excluded from default but accessible, dates preserved, no new sums or record reassignment, pending edits across sections, preview includes hidden edits, optional notes, read-only, navigation/dirty guards and no overflow. Existing review/navigation/history tests, build, exact generated chunk comparison, public artifact and anonymous startup with/without SW. Authenticated production acceptance not claimed; existing automated login unavailable. Full global asset regression NOT APPLICABLE: no shared shell/Auth/assets/repositories/SW logic change.
