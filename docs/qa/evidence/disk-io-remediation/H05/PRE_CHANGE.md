# H05 — Pre-change audit

H01/H02/H03/H04 STATUS: PASS comprobados en sus VERIFICATION.md antes de iniciar. Navigator FRESH y lookup get_self_savings_live_readonly completados, seguidos de discovery dirigido live/código. Autoridad explícita: solicitud H05 y Protocolo Maestro. No H06.

Objetivo: mismo resultado financiero completo, misma identidad/permisos/históricos y menos lecturas/trabajo. El lector live conserva MD5 74f504337e4f336e6b12da3ab32f30a6 y el contrato GOOGLE_LEGACY_AUTHORITY / SHADOW_MIRROR. No cutover, recálculo, corrección de datos, nuevo saldo o regla financiera.

Alcance: índices de savings_legacy_evidence; lector get_self_savings_live_readonly y dependencias para análisis; mecanismo condicional de reutilización delegante que preserve el payload original; SavingsRepository y savingsStore para memoria efímera, invalidación y descarte de respuestas antiguas. Leer consumidores Inicio/Finanzas/Ahorro, writers Savings y Auth/Admin/impersonación. No editar fórmulas, Google, Apps Script, reglas, RLS existente ni funciones financieras existentes.

Archivos autorizados previstos: docs/qa/evidence/disk-io-remediation/H05/**; supabase/migrations y recovery/20260907000500_savings_read_work.sql; app/savings-repository.js, app/savings-store.jsx; pruebas scripts/test-savings-read-work.js y scripts/test-savings-read-work-browser.js si necesarias. app/bundle.js y versiones SutiApp.html/sw.js sólo como artefactos generados. Registry derivado, índice de remediación y apéndice AGENT_CHANGELOG. Ampliar auditoría antes de editar otra fuente. Checkout/build públicos aislados C:/tmp/sutiapp-h05-release-20260907 y C:/tmp/sutiapp-h05-20260907-site.

El workspace contiene cambios Savings previos no publicados. Capturar hashes completos y diffs focales contra root HEAD y commit publicado 19eb53de6b3e6aef4e31a7435cb5643340b35589. Preservarlos; el build publicado incluirá únicamente H05. La protección de identidad/generación necesaria se verifica en ambas variantes sin incorporar métodos operativos financieros pendientes ajenos a H05.

Datos/autoridad: espejo certificado Supabase; Google mantiene autoridad histórica según ADR-095 y reglas posteriores vigentes. Los resultados BEFORE completos contienen finanzas/PII: comparar en memoria o dentro de una transacción y guardar únicamente hashes, estructura, diferencias por path sin valores, tamaños y evidencia de igualdad. No versionar payloads privados sin protección. La captura completa se conserva durante la comparación; no sustituirla por comparación parcial de saldos.

Plan de índice: EXPLAIN ANALYZE acotado de los predicados reales y de la RPC, comparar (participant_id,record_type,observed_on DESC) contra (participant_id,record_type,source_sheet,source_row DESC). Ensayos DDL en transacciones revertidas, lock_timeout 2 s y statement timeouts. No crear ambos productivamente. Elegir la estructura mínima demostrada; no cambiar ordenamientos ni reglas del getter.

Plan de reutilización: no aceptar TTL como prueba de frescura. Investigar validación backend por statement que derive principal, afiliado, impersonación, sesión, contexto y versión de todas las dependencias relevantes, incluidos cambios de fecha/acciones temporales. Una respuesta reutilizada debe haber sido validada; fallo retira datos, nunca fallback. Logout/cambio de sujeto elimina memoria; escrituras relacionadas invalidan aun ante resultado incierto. No persistencia financiera en browser ni autoridad del cliente.

Pruebas: casos reales representativos de procesos, saldo nulo, evidencia/tipos/fechas, retiros, cambios y sin participante, con captura completa campo por campo. Seguridad owner/otro/anon/impersonación y sin objetivo arbitrario. Invalidación por sujetos/sesión/contexto, escrituras y cambios subyacentes. No ejecutar operaciones financieras productivas de prueba. Para pruebas de invalidación, preferir fixtures aislados o transaccionales nuevos sin modificar históricos, conservando triggers y rollback. Auditar específicamente antes de insertar fixtures.

Riesgo y recuperación: índices pueden cambiar planes/orden de empates; exigir igualdad estructural, arrays incluidos. Un cache sin dependencia o fecha omitiría cambios: mapear todas las lecturas antes de implementarlo. Registrar defs, owner/ACL/SECURITY/search_path, RLS, constraints, triggers, índices, hashes de tablas financieras. Recovery de objetos exclusivos H05 con guards y orden frontend/backend compatible con clientes abiertos.

Guardians: source-of-truth, legacy-google, migration, Supabase security; clasificación inicial READ ONLY / REQUIRES AUDIT para candidatos. SAFE CHANGE sólo después de equivalencia. UI preservation y post-change antes de cerrar. Por repositorio/helper compartido entre Inicio/Finanzas/Ahorro, ejecutar regresión global local/GitHub Pages con assets legítimos. Architect review posterior; sin continuidad automática a otra H.

## Extension: rollback-only invalidation fixtures

Audited columns, constraints and append-only triggers in BASELINE.json. New rows only: one participant-scoped savings_action_availability, one savings_beneficiary_versions plus beneficiary, one RAW_LEGACY REPORT savings_legacy_evidence under an H05-only source key. All in BEGIN/ROLLBACK, never visible to other sessions, never modify existing history, never disable triggers. A new impersonation session and its normal audit may be created by existing authorized RPC inside the same rollback; no Auth/affiliate/role edits. These fixtures test dependency invalidation separately from financial equivalence on unchanged real inputs. No financial operation, ledger, Google or recalculation. Recovery uses transaction rollback.

Browser coverage extension: the available controlled login has no savings participant. Test both its empty state and one real certified participant through the existing authorized impersonation RPC, followed by stop_affiliate_impersonation in finally. Only normal identity session/audit writes; no financial data mutation, no role change and no auth reset. Refuse to start if this actor already has an active impersonation session. No identifiers, names or private payload exported.

Browser preflight found one expired identity session, zero unexpired sessions. The existing start RPC normally closes expired sessions and audits that transition. Permit that standard identity lifecycle during this test; continue refusing any unexpired active session. No manual session update/delete, no financial history change. This supersedes the conservative all-open-session check above.

Test maintenance extension: scripts/test-savings-shadow-foundation.js asserts the former direct RPC name in SavingsRepository. Update that single assertion to verify the new conditional RPC and its SQL delegation to the unchanged original getter, retaining the certified-Q checks. The root workspace has a separate pre-existing pending-identity assertion failure in unpublished Admin Savings; preserve it and verify the published isolated baseline/release independently.
