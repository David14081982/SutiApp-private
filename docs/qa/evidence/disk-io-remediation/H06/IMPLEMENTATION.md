# H06 — implementación

ProgramCatalogRepository admite programKey/itemId, includeAssets:false y deferImages:true. Primero consulta productos bajo RLS; consulta vínculos sólo para esos IDs. El contrato completo por defecto sigue disponible y fue comparado campo por campo mediante hashes del JSON completo (sin URLs temporales). No cambian proyección comercial, precios, estados, orden ni writers. El selector de Préstamo conserva el mismo find y payload; sólo evita productos e imágenes ajenos.

catalogStore carga un programa cuando su pantalla lo solicita; Marketplace conserva sus lectores originales. Admin consulta los 135 metadatos necesarios para sus conteos y obtiene vínculos al seleccionar programa. Se elimina el doble bootstrap/retry inicial observado. Una recarga se entrega completa; fallar la carga de vínculos conserva la proyección anterior y muestra error. Activar/guardar no recibe imágenes vacías como sustituto de una lectura fallida. Se conserva el array completo de vínculos históricos del editor.

PrivateResourceDemand coordina exclusivamente promesas simultáneas por actor, token/sesión, afiliado efectivo, impersonación y asignación/contexto Admin. El token se compara dentro de un closure y no se expone ni persiste. Las generaciones descartan respuestas tardías. Los stores se vacían al cambiar contexto. Fallos tardíos de favoritos no repueblan el nuevo contexto. Las escrituras siguen en sus repositories/RPC originales.

ProgramCatalogImage observa área visible positiva y pestaña visible. Firma sólo recursos demandados; agrupa las demandas simultáneas por bucket y deduplica un asset idéntico en vuelo. Conserva la firma únicamente en la instancia montada, bajo su contexto y deadline original de 3.600 segundos. Cambiar asset aunque conserve link_id invalida la URL. No hay caché global de firmas asentadas. Galería y editor conservan todos los slots, orden y controles; el visor compartido admite un componente de imagen opcional para resolver sólo la seleccionada. Su contrato de URLs y sus gestos/zoom/cierre permanecen intactos.

DocumentRequirementList sólo autoriza miniaturas en la variante que las representa. Su dependencia es el contenido seleccionado y propósito, más contexto; no la referencia de arrays. Sólo solicita las visibles y no renueva en una pestaña oculta. Conserva deadline máximo 300 segundos, una renovación ante error de imagen y rechazo de respuestas de otro contexto. Abrir sigue llamando selfPreview de nuevo. DocumentWorkflowRepository sólo deduplica peticiones exactamente simultáneas por documento, modo, propósito y target, dentro del mismo contexto. Edge document-access v4, autorización, auditoría y TTL no cambian.

No SQL, índices, RLS, grants, owner, Edge, Auth global, public bucket, nuevas reglas financieras, datos históricos ni Google. El release aislado parte de c047eec (H05), añade un módulo y cambia únicamente los consumidores declarados; los trabajos Savings pendientes del workspace quedan fuera. Bundle223 / worker170.
