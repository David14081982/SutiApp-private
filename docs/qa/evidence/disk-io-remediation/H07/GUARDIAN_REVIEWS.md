# H07 — diseño y autorización

SOURCE OF TRUTH AUDIT: SAFE. Companies/promotions siguen en Supabase con RLS. Nueva lectura embebida usa empresas obtenidas por el caller, hasta 100 UUID por petición y misma proyección/orden de promociones. El límite PostgREST live es 1.000; el embed conserva límite por empresa, evitando truncar todo el conjunto a 1.000. No nueva caché ni persistencia. Exports conserva allowlist, filters, MAX_ROWS, paging y auditoría. Auth email se obtiene sólo por auth_user_id exacto; histórico/numero_control no participan en el lookup. Ningún nuevo escritor de datos.

DATABASE MIGRATION AUDIT: PASS candidato. Una función aditiva STABLE SECURITY DEFINER search_path vacío, owner postgres; EXECUTE exclusivamente service_role. No tablas/views/índices/policies ni datos modificados. JSONB escalar acotado a 1.000 UUID evita truncación por límite de filas del API; respuesta validada por cardinalidad, UUID esperado único y email string. Backend tests: 13 checks, grants, filas vinculadas, null, faltante, 1.000/1.001 y forward/recovery con ROLLBACK. Baseline capturó owner/ACL/schema/constraints; 123 vínculos sin FK rota. Edge verifica el permiso antes de usar el cliente privilegiado y audita antes de entregar el archivo, igual que antes. Recuperación primero restaura Edge y después elimina RPC; no toca auditoría.

SUPABASE SECURITY REVIEW: PASS candidato. No grants Auth/Storage/browser nuevos; JWT Edge true. Proyección backend tiene los mismos privilegios del cliente service existente y sólo retorna id/email de los UUID solicitados. Ningún UUID, secreto, email real ni export privado en evidencia pública del repo. Matriz portal transaccional 16 casos (0/1/2/3 empresas, principal/member/otro/anon), resultado completo y denegaciones equivalentes, cero mezcla por company_id. Declaraciones de permisos Edge/domains/canExport permanecen iguales y se prueban en el harness. Una apertura documental explícita reautoriza; deduplicación sólo simultánea y por propósito/target/contexto; TTL300 sin cambio.

LEGACY GOOGLE AUDIT: SAFE CHANGE del lector de exportación, sin escritura financiera ni legacy. Todos los dominios/columnas/headers permanecen iguales; los campos financieros existentes sólo pasan por su serialización anterior. Cero cálculos/conciliaciones/Google/Apps Script. Históricos se comparan y no se corrigen. Baseline CSV/XLSX real de 954 filas capturado cifrado fuera del repo; comparación completa pendiente del deploy.

CLAUDE UI PRESERVATION REVIEW: contrato conservado en fuentes. CompanyScreen/CoPromos y pantalla Export no se modifican. DocumentCard mantiene textos, tarjeta/imagen, botón, estado y viewer; sólo demanda visible y expiración. Hash de texto/controles en React BEFORE/AFTER idéntico. Scroll, apertura, expiración, falta de permiso y logout comprobados aisladamente. Regresión global local/publicada requerida para cierre.

Referencias técnicas verificadas: [embeds y orden por recurso](https://docs.postgrest.org/en/v13/references/api/resource_embedding.html), [límite de filas](https://docs.postgrest.org/en/v13/references/configuration.html), [formato ESZIP](https://raw.githubusercontent.com/denoland/deno/main/libs/eszip/v2.rs). El bundle Edge live v11 se extrajo y su AST coincide con la fuente baseline; archivo completo recuperable en C:/tmp/sutiapp-h07-data-exports-v11.eszip. No se publican sus 30 MB de dependencias como evidencia redundante.

Estos veredictos autorizan la implementación ya solicitada; el PASS final requiere recibos live de implementación, equivalencia y regresión en VERIFICATION.md.

Verificación backend posterior: Edge v13, AST exacto y SDK2.115.0 del baseline. RPC agrega el predicado instance_id=UUID cero usado por [Auth FindUserByID](https://raw.githubusercontent.com/supabase/auth/master/internal/models/user.go); los 135 usuarios existentes coinciden. Fixture temporal de la función, con sólo la relación redirigida, excluye otra instancia/null sin mutar Auth. export-final.json compara campo por campo el XLSX y bytes CSV completos; mismo encabezado y auditoría. rpc-http.json demuestra service_role permitido y anónimo/Admin browser denegados. backend-integrity.json conserva las 253 funciones previas, 228 policies, 954 afiliados, 33 empresas y hashes Auth. No se toca un grant de tabla ni política.

El límite del embed se comprobó además en [treeRestrictRange de PostgREST](https://raw.githubusercontent.com/PostgREST/postgrest/v13.0.7/src/PostgREST/Plan.hs): se aplica a cada nodo del árbol de lectura. Test aislado con 1.001 promociones en cada una de tres empresas conserva exactamente 1.000 por empresa, como las consultas individuales; no se usa una lectura plana truncada globalmente.
