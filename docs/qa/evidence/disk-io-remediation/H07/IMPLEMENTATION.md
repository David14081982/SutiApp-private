# H07 — implementación

Portal: nuevo listPromotionsForCompanies consulta empresas en lotes de hasta 100 UUID e incrusta sus promociones mediante FK y RLS. El orden sort_order se aplica dentro de cada empresa, con el mismo límite PostgREST por recurso. companyStore recibe la proyección anterior y agrupa mediante company_id; valida correspondencia y falla cerrado ante respuesta ajena/contexto perdido. listPromotions individual, writers, planes, solicitudes, propuestas y pantallas permanecen iguales. No duplica la consulta por empresa.

Exportaciones: la paginación de tabla, allowlist, filtros, límite20.000, serialización, headers y auditoría permanecen exactos. Después de reunir los mismos UUID Auth únicos, data-exports solicita lotes de hasta1.000 a get_data_export_auth_emails. Función STABLE, owner postgres, SECURITY DEFINER/search_path vacío, sólo service_role. Lee email por UUID e instancia Auth exactos; no interpreta historical_email_raw, numero_control ni affiliate_id. El Edge valida UUIDs, duplicados, cardinalidad y tipo de email; conserva AUTH_EMAIL_LOOKUP_FAILED ante relación incompleta. Resultado interno se elimina antes de serializar las mismas columnas de exportación. SDK fijado a2.115.0, que era la dependencia efectiva del Edge baseline, para evitar upgrade incidental del import abierto.

Miniaturas: DocumentCard Admin usa el coordinador H06, demanda visible y TTL300. Conserva tarjeta/campos/estados/viewer y autorización por documento. Array nuevo con mismos valores no causa nueva firma. Contexto/objetivo/versión invalidan la fuente; la apertura explícita vuelve al backend y conserva auditoría. El observador compartido y su equivalente documental añaden un umbral positivo mínimo: resuelve la transición área cero→visible que una prueba de scroll detectó sin firmar imágenes que sólo tocan el borde.

Reordenamientos: ninguna modificación. Sus commits y fallos parciales impiden un batch transaccional equivalente. INVENTORY.md contiene callers, fórmulas y el caso de error que lo demuestra. Las RPC que ya reordenan atómicamente continúan en una llamada.

Release aislado desde a0159d7, cinco chunks modificados entre109, ninguno añadido ni eliminado. DocumentCard se transplanta sobre la fuente publicada; los otros cambios previos del archivo Admin/Savings quedan en el workspace. Build local bundle224/worker171. No nuevos índices/policies ni DML de negocio. La bitácora de exportación añade sólo sus eventos legítimos de pruebas; las fixtures RLS se revierten.
