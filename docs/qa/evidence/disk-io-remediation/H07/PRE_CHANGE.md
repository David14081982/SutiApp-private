# PRE-CHANGE AUDIT — H07

H: H07, N+1. Status: PASS para auditar, implementar y verificar; no es cierre H07.

AUDIT / AUTHORITY: H01–H06 PASS comprobados y Registry FRESH. Lookup data-exports/company portal y discovery dirigido identifican company-store → MarketplaceRepository.listPromotions, Edge data-exports.rowsFor, DocumentCard Admin y reordenamientos. Normativa leída en esta sesión, hashes vigentes comparados con H06; no cambió autoridad. Exportación es derivado efímero, con permiso data_exports.read/acciones por sección y auditoría existentes. auth.users.email sólo es correo Auth; affiliates.historical_email_raw y numero_control siguen históricos. No inferir vínculos por correo/nombre.

Objetivo / PLAN: sustituir C consultas de promociones por lectura de un conjunto explícito de empresas autorizado por RLS, preservando proyección y agrupación. Sustituir U getUserById por lotes acotados de una RPC backend service-only; conservar paginación, límite 20.000, columnas, encabezados, formato, canExport y auditoría. Investigar live el contrato Auth y recuperar fuente Edge antes de diseñar SQL definitivo. Completar demanda/expiración de miniaturas Admin mediante el coordinador H06 si la evidencia confirma la necesidad. Reordenamientos: preservar los que tengan commits parciales; una transacción única no conserva su error/atomicidad y no está autorizada por H07.

Archivos a tocar: app/company-store.jsx, app/marketplace-repository.js; supabase/functions/data-exports/index.ts; nueva supabase/migrations/20260907000600_n_plus_one_reads.sql y recovery homónimo. app/screens-admin-affiliates.jsx exclusivamente DocumentCard y wiring mínimo de demanda; no sus otros cambios previos. Tests focales nuevos scripts/test-n-plus-one-reads.js, scripts/test-n-plus-one-previews-browser.js; scripts/test-data-exports.js sólo si contiene aserciones literales obsoletas, sin retirar contratos. Artefactos bundle/HTML/SW y builder únicamente lo necesario para regenerar, Registry derivado; docs/qa/evidence/disk-io-remediation/H07/**, README y AGENT_CHANGELOG. Checkout aislado desde a0159d7 para excluir otros trabajos. Ampliar este alcance antes de editar archivos distintos.

Datos / tablas: lectura de companies, marketplace_promotions, company_portal_plans/subscriptions, marketplace_company_memberships, requests/quotes/propuestas; affiliates y auth.users por vínculo UUID exacto. data_export_audit_log mantiene INSERT legítimo por exportación de prueba. No guardar payload privado de exportaciones en el repo, ni URLs firmadas/secretos. Fixtures sintéticas exclusivamente aisladas o transaccionales revertidas; ningún producto, histórico, permiso real ni usuario de negocio se modifica.

Fuera de alcance: H08; writers/reordenamientos cuya semántica no pueda preservarse; cambios de identidad/permisos/Auth global/RLS/buckets/TTL/contratos comerciales; Google/Apps Script, cálculos/operaciones financieras, reimportación y limpieza incidental. Portales y exportaciones conservan componentes, textos, campos, layout, scroll, filtros, acciones y navegación. DocumentCard conserva tarjeta, miniatura, estado, nombre, botón y viewer; cambia exclusivamente cuándo se autoriza y renueva una imagen.

RISK: mezcla entre empresas, truncación de batch por límite PostgREST, orden con empates, UUID/email histórico confundidos, export sin auditoría, timeout, respuestas vencidas o de otra identidad. Validar límites/paginación reales, comparación completa BEFORE/AFTER, fail-closed, grants y objeto SQL sólo service_role. Capturar firma/owner/search_path/grants/schema/functions/policies/Edge live. Si el batch no puede preservar truncación/orden/error, ajustar diseño antes de aplicar.

VERIFY / EVIDENCE: portal 0/1/múltiples, sin/con promociones y roles; export CSV/XLSX completo BEFORE/AFTER en memoria (sólo hashes sanitizados), pocos/múltiples páginas, vinculados/históricos/Auth/denegado/20.000 y exceso; miniaturas 0/1/N, demanda, referencias repetidas, expiración y acceso negado. Reordenamientos antes/después analizados con fallos parciales sin alterar producción. Tests aislados y matriz backend real con ROLLBACK, build, regresión global local/GitHub Pages porque MarketplaceRepository y DocumentCard son consumidores compartidos. Checks de invariantes, secretos, writers, workspace y Registry. Reviewer independiente al cierre.

Recovery: fuente Edge live guardada y redeploy verificado antes de retirar RPC nueva; rollback SQL sólo retira infraestructura nueva después del consumidor. Ninguna auditoría se borra. Frontend desde a0159d7 con cachebusters nuevos, preservando H01–H06 y trabajo pendiente ajeno. Ensayar forward/recovery en ROLLBACK y probar deploy bundle antes de apply. No PASS sin evidencia y recuperación disponibles.

Ampliación auditada antes de editar: app/private-resource-demand.js y app/screens-documentos.jsx, exclusivamente el threshold del IntersectionObserver. La prueba React H07 encontró que threshold=0 puede notificar isIntersecting=true con área cero y no volver a notificar al entrar; el filtro de área positiva H06 dejaba entonces una miniatura sin cargar. Añadir un segundo umbral positivo diminuto conserva la demanda visible y provoca la notificación requerida. Capturar baseline de screens-documentos; shared helper ya capturado. Tests focales H06/H07 y regresión global obligatoria cubrirán la corrección. No cambio de RLS/firma/TTL ni contenido.

Refinamiento auditado del backend: ESZIP v11 resolvía supabase-js2.115.0, pero el import abierto @2 produjo 2.116.0 al desplegar v12. Fijar el import Edge a 2.115.0 para conservar la dependencia efectiva previa, verificar AST/SDK tras redeploy y repetir equivalencia. La fuente Auth FindUserByID también filtra instance_id=UUID cero; añadir exactamente ese predicado al helper evita ampliar el lookup a otra instancia. Los 135 usuarios live tienen UUID cero (auth-instance-baseline.json). No se cambia identidad ni datos; recuperar el baseline Edge con el mismo SDK fijado. Misma RPC/archivos declarados, grants y salida.
