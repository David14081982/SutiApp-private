# H07 — pruebas

17 suites focales pasan tanto en workspace como en el checkout aislado de publicación: tests-scoped-workspace.json y tests-scoped-release.json. Incluyen catálogo, contratos comerciales, assets privados/demanda, documentos, exports y los nuevos tests N+1 de lectura y navegador React. Build 224/SW171; build-proof.json confirma exactamente cinco módulos fuente cambiados entre 109 chunks y preservación del trabajo previo del workspace.

scripts/test-n-plus-one-reads.js compara respuestas completas, campos CSV de todos los dominios, paginación y límite 20.000/20.001, permisos, errores de lookup, UUID equivocado/duplicado/ausente, correos vacíos, auditoría y ausencia de entrega ante fallo. Portal: 0/1/33/101 empresas, pertenencia estricta, promociones no vacías, límite por empresa y commits parciales de reordenamiento. scripts/test-n-plus-one-previews-browser.js compara texto/controles, 0/1/N, scroll, renders repetidos, apertura, contexto/logout, expiración y denegación sin loop. Fixtures exclusivamente aisladas.

Backend real: portal-security.json contiene 16 comparaciones de filas completas y denegaciones con RLS en transacción revertida; backend-candidate/recovery y rpc-http comprueban firma, ACL, límites y rechazo a anon/authenticated. auth-instance-candidate.json comprueba exclusión de instancias ajenas en relación temporal. export-final.json certifica CSV byte por byte y XLSX campo por campo de 954 afiliados, incluidos 123 vínculos Auth y 61 correos Auth distintos del histórico. Los archivos privados se comparan en memoria contra baseline cifrado fuera del repo; evidencia sólo con hashes/conteos.

Regresión global local: global-local.json PASS, assets legítimos, Admin, documentos imagen/PDF, Membership, Préstamo, Marketplace, fullscreen, refresh y con/sin SW. Admin real adicional: diez miniaturas al recorrerlas, mismo texto y apertura reautorizada. Publicación y regresión pública se documentan en VERIFICATION.md cuando concluyan.

Limitaciones y fallos preservados: test-admin-affiliates.js falla en el baseline publicado a0159d7 y en el checkout H07 por una aserción de Finanzas ajena al DocumentCard; admin-affiliates-baseline-test.json conserva el fallo idéntico. Pasa en el workspace que ya contiene trabajo financiero pendiente; ese trabajo no se publica para resolver la prueba. El portal completo retorna el mismo 42501 program_requests antes de promociones; no se amplían permisos. El lector autorizado sí se compara real, y el store completo con fixtures aisladas válidas.

La primera regresión global local sufrió net::ERR_CONNECTION_CLOSED; recibo conservado en global-local-first-failure.json. La repetición completa pasó sin cambiar runtime por ese fallo de red. Los ajustes del harness de navegador sincronizan expiración/observer asíncronos; el bug real de área cero se corrigió con umbral positivo y pasó todos los recorridos.

Registry: scripts/test-architecture-registry.py terminó Exit 0, PASS generación/freshness/stale/lookup/reversos/permisos/incremental/secretos/determinismo. El wrapper posterior intentó tratar registry-edges.json regenerado como fuente inmutable y falló. Se corrigió únicamente el wrapper para excluir los cinco outputs derivados. El check posterior fue FRESH; no se altera el generador ni se presenta el fallo del wrapper como fallo de la suite.
