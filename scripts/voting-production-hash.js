'use strict';
const fs=require('fs'),crypto=require('crypto'),path=require('path'),assert=require('assert/strict');
const out=path.resolve(__dirname,'../docs/qa/evidence/voting-20260915');
(async()=>{const base='https://david14081982.github.io/SutiApp-private/',html=await fetch(base+'SutiApp.html?verify=voting-002',{cache:'no-store'});assert.equal(html.status,200);const body=await html.text();assert(body.includes('app/bundle.js?v=voting-20260915-002'));const bundle=await fetch(base+'app/bundle.js?v=voting-20260915-002',{cache:'no-store'});assert.equal(bundle.status,200);const hash=crypto.createHash('sha256').update(Buffer.from(await bundle.arrayBuffer())).digest('hex');assert.equal(hash,JSON.parse(fs.readFileSync(path.join(out,'build.json'),'utf8')).sha256);const result={status:'PASS',version:'voting-20260915-002',sha256:hash,commit:'5aecd332fd9c5cd703ec6bfa2b097300e4b8a3ab',deployment:'https://github.com/David14081982/SutiApp-private/actions/runs/35047615354'};fs.writeFileSync(path.join(out,'deployment.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result));})().catch(e=>{console.error(e.message);process.exitCode=1;});
