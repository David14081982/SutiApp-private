-- H01: forward; 10 reviewed functions, 12 deterministic business conflicts only.
-- No table, policy, grant, lock, business calculation, or successful result changes.
-- Guards reject unknown definitions/security metadata; idempotent on the exact expected result.
BEGIN;
SET LOCAL lock_timeout='2s';
SET LOCAL statement_timeout='15s';
DO $h01$
DECLARE item jsonb; current_def text; proc record;
BEGIN
  PERFORM pg_advisory_xact_lock(hashtext('SUTIAPP_H01_SQLSTATE'));
  FOR item IN SELECT value FROM jsonb_array_elements($h01_targets$[{"signature":"public.archive_admin_affiliate(uuid,timestamp with time zone,text)","owner":"postgres","acl":"{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"f97e883ce8c3fefa2aa785cf7fdbf11b","result":"97ab7cb6cc5bda40b17dd623f0494715","definition":"CREATE OR REPLACE FUNCTION public.archive_admin_affiliate(p_affiliate_id uuid, p_expected_updated_at timestamp with time zone, p_reason text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare v_old public.affiliates%rowtype;v_new public.affiliates%rowtype;v_reason text:=btrim(coalesce(p_reason,''));\nbegin\n  if not public.has_admin_permission('affiliates.write') then raise exception 'AFFILIATE_WRITE_DENIED' using errcode='42501'; end if;\n  if length(v_reason) not between 8 and 500 then raise exception 'AFFILIATE_ARCHIVE_REASON_REQUIRED' using errcode='22023'; end if;\n  select * into v_old from public.affiliates where id=p_affiliate_id for update;\n  if v_old.id is null then raise exception 'AFFILIATE_NOT_FOUND' using errcode='P0001'; end if;\n  if v_old.updated_at is distinct from p_expected_updated_at then raise exception 'AFFILIATE_VERSION_CONFLICT' using errcode='PT409'; end if;\n  if v_old.is_archived then raise exception 'AFFILIATE_ALREADY_ARCHIVED' using errcode='22023'; end if;\n  update public.affiliates set is_archived=true,archived_at=now(),archived_by_auth_user_id=auth.uid(),\n    archive_reason=v_reason,archive_previous_status_raw=coalesce(affiliate_status_raw,'Sin estado'),\n    restored_at=null,restored_by_auth_user_id=null,restore_reason=null\n  where id=p_affiliate_id returning * into v_new;\n  update public.impersonation_sessions set ended_at=now(),ended_by_auth_user_id=auth.uid()\n   where usuario_contexto_affiliate_id=p_affiliate_id and ended_at is null;\n  insert into public.affiliate_admin_events(affiliate_id,actor_auth_user_id,action,before_values,after_values,changed_fields,reason)\n  values(p_affiliate_id,auth.uid(),'ARCHIVE',\n    jsonb_build_object('is_archived',false,'affiliate_status_raw',v_old.affiliate_status_raw),\n    jsonb_build_object('is_archived',true,'archived_at',v_new.archived_at,'archive_previous_status_raw',v_new.archive_previous_status_raw),\n    array['is_archived','archived_at','archived_by_auth_user_id','archive_reason','archive_previous_status_raw'],v_reason);\n  return public.get_admin_affiliate_workbench(p_affiliate_id);\nend $function$\n"},{"signature":"public.change_admin_affiliate_status(uuid,timestamp with time zone,text,text)","owner":"postgres","acl":"{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"15951e0aaf52278ba6236715d9993d72","result":"69b45f8ba9280a08f23b3b5a3edf3344","definition":"CREATE OR REPLACE FUNCTION public.change_admin_affiliate_status(p_affiliate_id uuid, p_expected_updated_at timestamp with time zone, p_new_status text, p_reason text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare v_old public.affiliates%rowtype;v_new public.affiliates%rowtype;v_status text:=btrim(coalesce(p_new_status,''));v_reason text:=btrim(coalesce(p_reason,''));\nbegin\n  if not public.has_admin_permission('affiliates.write') then raise exception 'AFFILIATE_WRITE_DENIED' using errcode='42501'; end if;\n  if length(v_reason) not between 8 and 500 then raise exception 'AFFILIATE_REASON_REQUIRED' using errcode='22023'; end if;\n  if v_status='' or not exists(select 1 from public.affiliates where affiliate_status_raw=v_status) then raise exception 'AFFILIATE_STATUS_INVALID' using errcode='22023'; end if;\n  select * into v_old from public.affiliates where id=p_affiliate_id for update;\n  if v_old.id is null then raise exception 'AFFILIATE_NOT_FOUND' using errcode='P0001'; end if;\n  if v_old.updated_at is distinct from p_expected_updated_at then raise exception 'AFFILIATE_VERSION_CONFLICT' using errcode='PT409'; end if;\n  if v_old.affiliate_status_raw is not distinct from v_status then raise exception 'AFFILIATE_STATUS_NO_CHANGE' using errcode='22023'; end if;\n  update public.affiliates set affiliate_status_raw=v_status where id=p_affiliate_id returning * into v_new;\n  insert into public.affiliate_admin_events(affiliate_id,actor_auth_user_id,action,before_values,after_values,changed_fields,reason)\n  values(p_affiliate_id,auth.uid(),'STATUS_CHANGE',jsonb_build_object('affiliate_status_raw',v_old.affiliate_status_raw),jsonb_build_object('affiliate_status_raw',v_new.affiliate_status_raw),array['affiliate_status_raw'],v_reason);\n  return public.get_admin_affiliate_workbench(p_affiliate_id);\nend $function$\n"},{"signature":"public.create_validated_financial_program_request(uuid,uuid,uuid,uuid,text,text,uuid,uuid[],uuid,numeric,integer,text,integer,jsonb)","owner":"postgres","acl":"{postgres=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"ab067d9e36b9a9e8e11e8fc6e44ce123","result":"f6aa8e78687abf16a81d06030659304e","definition":"CREATE OR REPLACE FUNCTION public.create_validated_financial_program_request(p_actor_real_auth_user_id uuid, p_affiliate_id uuid, p_impersonation_session_id uuid, p_program_item_id uuid, p_notes text, p_signature_data text, p_terms_version_id uuid, p_document_ids uuid[], p_idempotency_key uuid, p_amount numeric, p_term integer, p_term_semantics text, p_expected_profile_version integer, p_financial_submission_snapshot jsonb)\n RETURNS program_requests\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare\n  v_affiliate public.affiliates%rowtype;\n  v_item public.program_catalog_items%rowtype;\n  v_ctx public.impersonation_sessions%rowtype;\n  v_row public.program_requests%rowtype;\n  v_deposit public.loan_request_deposit_snapshots%rowtype;\n  v_submission jsonb;\n  v_phone text;\n  v_bank_text text;\n  v_missing integer;\nbegin\n  v_bank_text:=nullif(btrim(coalesce(p_financial_submission_snapshot->'deposit_selection'->>'bank_account_id','')),'');\n  if v_bank_text is not null then\n    return public.create_validated_financial_program_request_bank_required(\n      p_actor_real_auth_user_id,p_affiliate_id,p_impersonation_session_id,p_program_item_id,p_notes,\n      p_signature_data,p_terms_version_id,p_document_ids,p_idempotency_key,p_amount,p_term,\n      p_term_semantics,p_expected_profile_version,p_financial_submission_snapshot\n    );\n  end if;\n\n  if coalesce(auth.role(),'')<>'service_role' then raise exception 'SERVICE_ROLE_REQUIRED' using errcode='42501'; end if;\n  if p_actor_real_auth_user_id is null or not exists(select 1 from auth.users where id=p_actor_real_auth_user_id) then\n    raise exception 'AUTH_ACTOR_REQUIRED' using errcode='42501';\n  end if;\n  select * into v_affiliate from public.affiliates where id=p_affiliate_id for update;\n  if v_affiliate.id is null or v_affiliate.financial_profile_version<>p_expected_profile_version then\n    raise exception 'CONDITIONS_CHANGED' using errcode='PT409';\n  end if;\n  if p_impersonation_session_id is null then\n    if v_affiliate.auth_user_id is distinct from p_actor_real_auth_user_id then raise exception 'AFFILIATE_CONTEXT_DENIED' using errcode='42501'; end if;\n  else\n    select * into v_ctx from public.impersonation_sessions where id=p_impersonation_session_id for share;\n    if v_ctx.id is null or v_ctx.actor_real_auth_user_id<>p_actor_real_auth_user_id\n       or v_ctx.usuario_contexto_affiliate_id<>p_affiliate_id or v_ctx.ended_at is not null or v_ctx.expires_at<=now()\n       or not exists(select 1 from public.admin_assignments a join public.admin_roles r on r.id=a.role_id and r.enabled where a.auth_user_id=p_actor_real_auth_user_id and a.enabled) then\n      raise exception 'IMPERSONATION_CONTEXT_INVALID' using errcode='42501';\n    end if;\n  end if;\n  select * into v_item from public.program_catalog_items\n   where id=p_program_item_id and program_key='prestamo' and enabled and request_mode='supabase' and legacy_boundary;\n  if v_item.id is null then raise exception 'PROGRAM_NOT_REQUESTABLE' using errcode='22023'; end if;\n  if p_idempotency_key is null then raise exception 'IDEMPOTENCY_KEY_REQUIRED' using errcode='22023'; end if;\n  if nullif(btrim(coalesce(p_signature_data,'')),'') is null then raise exception 'SIGNATURE_AND_TERMS_REQUIRED' using errcode='22023'; end if;\n  if p_amount is null or p_amount<=0 or p_term is null or p_term<=0 or length(btrim(coalesce(p_term_semantics,''))) not between 3 and 80 then\n    raise exception 'FINANCIAL_REQUEST_TERMS_INVALID' using errcode='22023';\n  end if;\n  if not exists(select 1 from public.program_terms_versions where id=p_terms_version_id and program_id='prestamo' and membership_offering_id is null and published) then\n    raise exception 'TERMS_VERSION_REQUIRED' using errcode='22023';\n  end if;\n  if p_financial_submission_snapshot is null\n     or jsonb_typeof(p_financial_submission_snapshot)<>'object'\n     or not(p_financial_submission_snapshot ?& array[\n       'affiliate_id','actor_real_auth_user_id','profile_version','profile_fingerprint',\n       'criteria_source_fingerprint','term_policy_fingerprint','calculation_contract_version',\n       'criterion_identity','financialResult','confirmed_at','deposit_selection'\n     ])\n     or jsonb_typeof(p_financial_submission_snapshot->'financialResult')<>'object'\n     or jsonb_typeof(p_financial_submission_snapshot->'deposit_selection')<>'object'\n     or nullif(btrim(coalesce(p_financial_submission_snapshot->'deposit_selection'->>'bank_account_id','')),'') is not null\n     or p_financial_submission_snapshot->>'affiliate_id'<>p_affiliate_id::text\n     or p_financial_submission_snapshot->>'actor_real_auth_user_id'<>p_actor_real_auth_user_id::text\n     or coalesce(p_financial_submission_snapshot->>'impersonation_session_id','')<>coalesce(p_impersonation_session_id::text,'')\n     or (p_financial_submission_snapshot->>'profile_version')::integer<>p_expected_profile_version\n     or (p_financial_submission_snapshot->'financialResult'->>'amount')::numeric<>p_amount\n     or (p_financial_submission_snapshot->'financialResult'->>'paymentCount')::integer<>p_term then\n    raise exception 'FINANCIAL_SUBMISSION_CONTRACT_MISMATCH' using errcode='22023';\n  end if;\n  v_phone:=p_financial_submission_snapshot->'deposit_selection'->>'notification_phone';\n  if coalesce(v_phone,'') !~ '^[0-9]{10}$' then raise exception 'INVALID_NOTIFICATION_PHONE' using errcode='22023'; end if;\n\n  v_submission:=(p_financial_submission_snapshot-'deposit_selection') || jsonb_build_object('deposit',jsonb_build_object(\n    'bank_account_id',null,'bank_name',null,'masked_card',null,'masked_clabe',null,\n    'masked_phone','••• ••• '||right(v_phone,4)\n  ));\n\n  select count(*) into v_missing from public.program_document_requirements r\n   where r.program_id='prestamo' and r.membership_offering_id is null and r.enabled and r.required and not exists(\n     select 1 from public.affiliate_documents d where d.id=any(coalesce(p_document_ids,array[]::uuid[]))\n       and d.affiliate_id=p_affiliate_id and d.document_type_id=r.document_type_id\n       and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED'));\n  if v_missing>0 then raise exception 'REQUIRED_DOCUMENTS_MISSING' using errcode='22023'; end if;\n\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_id<>'prestamo'\n       or v_row.program_item_id<>p_program_item_id or v_row.requested_amount<>p_amount or v_row.requested_term<>p_term then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    select * into v_deposit from public.loan_request_deposit_snapshots where request_id=v_row.id;\n    if v_deposit.request_id is null or v_deposit.source_bank_account_id is not null or v_deposit.notification_phone<>v_phone then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n\n  insert into public.program_requests(\n    actor_real_auth_user_id,affiliate_id,usuario_contexto_affiliate_id,impersonation_session_id,impersonation_reason,\n    numero_control,program_id,program_item_id,request_type,status,quantity,notes,signature_data,terms_accepted,\n    terms_version_id,source_context,financial_processing_status,idempotency_key,requested_amount,requested_term,\n    requested_term_semantics,financial_submission_snapshot\n  ) values(\n    p_actor_real_auth_user_id,p_affiliate_id,case when v_ctx.id is null then null else p_affiliate_id end,v_ctx.id,v_ctx.reason,\n    v_affiliate.numero_control,'prestamo',v_item.id,'quote','requires_financial_processing',1,\n    left(nullif(btrim(coalesce(p_notes,'')),''),2000),p_signature_data,true,p_terms_version_id,\n    jsonb_build_object('source','sutiapp','financial_confirmation','SUPABASE_REVALIDATED','assisted',v_ctx.id is not null),\n    'pending',p_idempotency_key,p_amount,p_term,btrim(p_term_semantics),v_submission\n  ) returning * into v_row;\n\n  insert into public.loan_request_deposit_snapshots(request_id,affiliate_id,notification_phone)\n  values(v_row.id,p_affiliate_id,v_phone);\n  insert into public.sensitive_change_audit(actor_auth_user_id,affiliate_id,resource,action,target_id,metadata)\n  values(p_actor_real_auth_user_id,p_affiliate_id,'program_requests','LOAN_DEPOSIT_CONFIRMED',v_row.id,\n    jsonb_build_object('has_bank_account',false,'phone_last4',right(v_phone,4)));\n\n  insert into public.request_documents(request_id,document_type_id,affiliate_document_id,private_asset_id,asset_sha256,status_at_submission)\n  select v_row.id,d.document_type_id,d.id,coalesce(d.private_asset_id,af.private_asset_id),pa.content_sha256,d.status\n  from public.affiliate_documents d left join public.affiliate_files af on af.id=d.affiliate_file_id\n  join public.private_assets pa on pa.id=coalesce(d.private_asset_id,af.private_asset_id)\n  where d.id=any(coalesce(p_document_ids,array[]::uuid[])) and d.affiliate_id=p_affiliate_id\n    and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED')\n  on conflict(request_id,document_type_id) do nothing;\n  return v_row;\nexception when unique_violation then\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_id<>'prestamo'\n       or v_row.program_item_id<>p_program_item_id or v_row.requested_amount<>p_amount or v_row.requested_term<>p_term then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    select * into v_deposit from public.loan_request_deposit_snapshots where request_id=v_row.id;\n    if v_deposit.request_id is null or v_deposit.source_bank_account_id is not null or v_deposit.notification_phone<>v_phone then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n  raise;\nend $function$\n"},{"signature":"public.create_validated_financial_program_request_bank_required(uuid,uuid,uuid,uuid,text,text,uuid,uuid[],uuid,numeric,integer,text,integer,jsonb)","owner":"postgres","acl":"{postgres=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"32d8d3867f6a63ba248ec795a14ca495","result":"1def6bb403b85a21f5b02eb6658756d8","definition":"CREATE OR REPLACE FUNCTION public.create_validated_financial_program_request_bank_required(p_actor_real_auth_user_id uuid, p_affiliate_id uuid, p_impersonation_session_id uuid, p_program_item_id uuid, p_notes text, p_signature_data text, p_terms_version_id uuid, p_document_ids uuid[], p_idempotency_key uuid, p_amount numeric, p_term integer, p_term_semantics text, p_expected_profile_version integer, p_financial_submission_snapshot jsonb)\n RETURNS program_requests\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare\n  v_affiliate public.affiliates%rowtype;\n  v_item public.program_catalog_items%rowtype;\n  v_ctx public.impersonation_sessions%rowtype;\n  v_row public.program_requests%rowtype;\n  v_bank public.affiliate_bank_accounts%rowtype;\n  v_deposit public.loan_request_deposit_snapshots%rowtype;\n  v_submission jsonb;\n  v_phone text;\n  v_bank_id uuid;\n  v_missing integer;\nbegin\n  if coalesce(auth.role(),'')<>'service_role' then raise exception 'SERVICE_ROLE_REQUIRED' using errcode='42501'; end if;\n  if p_actor_real_auth_user_id is null or not exists(select 1 from auth.users where id=p_actor_real_auth_user_id) then\n    raise exception 'AUTH_ACTOR_REQUIRED' using errcode='42501';\n  end if;\n  select * into v_affiliate from public.affiliates where id=p_affiliate_id for update;\n  if v_affiliate.id is null or v_affiliate.financial_profile_version<>p_expected_profile_version then\n    raise exception 'CONDITIONS_CHANGED' using errcode='PT409';\n  end if;\n  if p_impersonation_session_id is null then\n    if v_affiliate.auth_user_id is distinct from p_actor_real_auth_user_id then raise exception 'AFFILIATE_CONTEXT_DENIED' using errcode='42501'; end if;\n  else\n    select * into v_ctx from public.impersonation_sessions where id=p_impersonation_session_id for share;\n    if v_ctx.id is null or v_ctx.actor_real_auth_user_id<>p_actor_real_auth_user_id\n       or v_ctx.usuario_contexto_affiliate_id<>p_affiliate_id or v_ctx.ended_at is not null or v_ctx.expires_at<=now()\n       or not exists(select 1 from public.admin_assignments a join public.admin_roles r on r.id=a.role_id and r.enabled where a.auth_user_id=p_actor_real_auth_user_id and a.enabled) then\n      raise exception 'IMPERSONATION_CONTEXT_INVALID' using errcode='42501';\n    end if;\n  end if;\n  select * into v_item from public.program_catalog_items\n   where id=p_program_item_id and program_key='prestamo' and enabled and request_mode='supabase' and legacy_boundary;\n  if v_item.id is null then raise exception 'PROGRAM_NOT_REQUESTABLE' using errcode='22023'; end if;\n  if p_idempotency_key is null then raise exception 'IDEMPOTENCY_KEY_REQUIRED' using errcode='22023'; end if;\n  if nullif(btrim(coalesce(p_signature_data,'')),'') is null then raise exception 'SIGNATURE_AND_TERMS_REQUIRED' using errcode='22023'; end if;\n  if p_amount is null or p_amount<=0 or p_term is null or p_term<=0 or length(btrim(coalesce(p_term_semantics,''))) not between 3 and 80 then\n    raise exception 'FINANCIAL_REQUEST_TERMS_INVALID' using errcode='22023';\n  end if;\n  if not exists(select 1 from public.program_terms_versions where id=p_terms_version_id and program_id='prestamo' and membership_offering_id is null and published) then\n    raise exception 'TERMS_VERSION_REQUIRED' using errcode='22023';\n  end if;\n  if p_financial_submission_snapshot is null\n     or jsonb_typeof(p_financial_submission_snapshot)<>'object'\n     or not(p_financial_submission_snapshot ?& array[\n       'affiliate_id','actor_real_auth_user_id','profile_version','profile_fingerprint',\n       'criteria_source_fingerprint','term_policy_fingerprint','calculation_contract_version',\n       'criterion_identity','financialResult','confirmed_at','deposit_selection'\n     ])\n     or jsonb_typeof(p_financial_submission_snapshot->'financialResult')<>'object'\n     or jsonb_typeof(p_financial_submission_snapshot->'deposit_selection')<>'object'\n     or p_financial_submission_snapshot->>'affiliate_id'<>p_affiliate_id::text\n     or p_financial_submission_snapshot->>'actor_real_auth_user_id'<>p_actor_real_auth_user_id::text\n     or coalesce(p_financial_submission_snapshot->>'impersonation_session_id','')<>coalesce(p_impersonation_session_id::text,'')\n     or (p_financial_submission_snapshot->>'profile_version')::integer<>p_expected_profile_version\n     or (p_financial_submission_snapshot->'financialResult'->>'amount')::numeric<>p_amount\n     or (p_financial_submission_snapshot->'financialResult'->>'paymentCount')::integer<>p_term then\n    raise exception 'FINANCIAL_SUBMISSION_CONTRACT_MISMATCH' using errcode='22023';\n  end if;\n  begin\n    v_bank_id:=(p_financial_submission_snapshot->'deposit_selection'->>'bank_account_id')::uuid;\n  exception when others then\n    raise exception 'INVALID_DEPOSIT_ACCOUNT' using errcode='22023';\n  end;\n  v_phone:=p_financial_submission_snapshot->'deposit_selection'->>'notification_phone';\n  if coalesce(v_phone,'') !~ '^[0-9]{10}$' then raise exception 'INVALID_NOTIFICATION_PHONE' using errcode='22023'; end if;\n  select * into v_bank from public.affiliate_bank_accounts\n   where id=v_bank_id and affiliate_id=p_affiliate_id and data_status='COMPLETE'\n     and (card_number ~ '^[0-9]{16}$' or public.is_valid_clabe(clabe))\n     and (card_number is null or card_number ~ '^[0-9]{16}$')\n     and (clabe is null or public.is_valid_clabe(clabe))\n   for share;\n  if v_bank.id is null then raise exception 'DEPOSIT_ACCOUNT_UNAVAILABLE' using errcode='42501'; end if;\n\n  v_submission:=(p_financial_submission_snapshot-'deposit_selection') || jsonb_build_object('deposit',jsonb_build_object(\n    'bank_account_id',v_bank.id,'bank_name',v_bank.bank_name,\n    'masked_card','•••• '||right(v_bank.card_number,4),\n    'masked_clabe','•••• •••• •••• ••'||right(v_bank.clabe,4),\n    'masked_phone','••• ••• '||right(v_phone,4)\n  ));\n\n  select count(*) into v_missing from public.program_document_requirements r\n   where r.program_id='prestamo' and r.membership_offering_id is null and r.enabled and r.required and not exists(\n     select 1 from public.affiliate_documents d where d.id=any(coalesce(p_document_ids,array[]::uuid[]))\n       and d.affiliate_id=p_affiliate_id and d.document_type_id=r.document_type_id\n       and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED'));\n  if v_missing>0 then raise exception 'REQUIRED_DOCUMENTS_MISSING' using errcode='22023'; end if;\n\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_id<>'prestamo'\n       or v_row.program_item_id<>p_program_item_id or v_row.requested_amount<>p_amount or v_row.requested_term<>p_term then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    select * into v_deposit from public.loan_request_deposit_snapshots where request_id=v_row.id;\n    if v_deposit.request_id is null or v_deposit.source_bank_account_id<>v_bank.id or v_deposit.notification_phone<>v_phone then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n\n  insert into public.program_requests(\n    actor_real_auth_user_id,affiliate_id,usuario_contexto_affiliate_id,impersonation_session_id,impersonation_reason,\n    numero_control,program_id,program_item_id,request_type,status,quantity,notes,signature_data,terms_accepted,\n    terms_version_id,source_context,financial_processing_status,idempotency_key,requested_amount,requested_term,\n    requested_term_semantics,financial_submission_snapshot\n  ) values(\n    p_actor_real_auth_user_id,p_affiliate_id,case when v_ctx.id is null then null else p_affiliate_id end,v_ctx.id,v_ctx.reason,\n    v_affiliate.numero_control,'prestamo',v_item.id,'quote','requires_financial_processing',1,\n    left(nullif(btrim(coalesce(p_notes,'')),''),2000),p_signature_data,true,p_terms_version_id,\n    jsonb_build_object('source','sutiapp','financial_confirmation','SUPABASE_REVALIDATED','assisted',v_ctx.id is not null),\n    'pending',p_idempotency_key,p_amount,p_term,btrim(p_term_semantics),v_submission\n  ) returning * into v_row;\n\n  insert into public.loan_request_deposit_snapshots(\n    request_id,affiliate_id,source_bank_account_id,bank_name,account_holder,card_number,clabe,notification_phone\n  ) values(\n    v_row.id,p_affiliate_id,v_bank.id,v_bank.bank_name,v_bank.account_holder,v_bank.card_number,v_bank.clabe,v_phone\n  );\n  insert into public.sensitive_change_audit(actor_auth_user_id,affiliate_id,resource,action,target_id,metadata)\n  values(p_actor_real_auth_user_id,p_affiliate_id,'program_requests','LOAN_DEPOSIT_SELECTED',v_row.id,\n    jsonb_build_object('bank_account_id',v_bank.id,'card_last4',right(v_bank.card_number,4),'clabe_last4',right(v_bank.clabe,4),'phone_last4',right(v_phone,4)));\n\n  insert into public.request_documents(request_id,document_type_id,affiliate_document_id,private_asset_id,asset_sha256,status_at_submission)\n  select v_row.id,d.document_type_id,d.id,coalesce(d.private_asset_id,af.private_asset_id),pa.content_sha256,d.status\n  from public.affiliate_documents d left join public.affiliate_files af on af.id=d.affiliate_file_id\n  join public.private_assets pa on pa.id=coalesce(d.private_asset_id,af.private_asset_id)\n  where d.id=any(coalesce(p_document_ids,array[]::uuid[])) and d.affiliate_id=p_affiliate_id\n    and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED')\n  on conflict(request_id,document_type_id) do nothing;\n  return v_row;\nexception when unique_violation then\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_id<>'prestamo'\n       or v_row.program_item_id<>p_program_item_id or v_row.requested_amount<>p_amount or v_row.requested_term<>p_term then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    select * into v_deposit from public.loan_request_deposit_snapshots where request_id=v_row.id;\n    if v_deposit.request_id is null or v_deposit.source_bank_account_id<>v_bank.id or v_deposit.notification_phone<>v_phone then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n  raise;\nend $function$\n"},{"signature":"public.create_validated_financial_program_request_v1_engine(uuid,uuid,uuid,uuid,text,text,uuid,uuid[],uuid,numeric,integer,text,integer,jsonb)","owner":"postgres","acl":"{postgres=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"de0c69b4f6a13d41644f75a2055635dc","result":"a034d98f78069ae3c81fa2b47eb16985","definition":"CREATE OR REPLACE FUNCTION public.create_validated_financial_program_request_v1_engine(p_actor_real_auth_user_id uuid, p_affiliate_id uuid, p_impersonation_session_id uuid, p_program_item_id uuid, p_notes text, p_signature_data text, p_terms_version_id uuid, p_document_ids uuid[], p_idempotency_key uuid, p_amount numeric, p_term integer, p_term_semantics text, p_expected_profile_version integer, p_financial_submission_snapshot jsonb)\n RETURNS program_requests\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare\n  v_affiliate public.affiliates%rowtype;\n  v_item public.program_catalog_items%rowtype;\n  v_ctx public.impersonation_sessions%rowtype;\n  v_row public.program_requests%rowtype;\n  v_missing integer;\nbegin\n  if coalesce(auth.role(),'')<>'service_role' then raise exception 'SERVICE_ROLE_REQUIRED' using errcode='42501'; end if;\n  if p_actor_real_auth_user_id is null or not exists(select 1 from auth.users where id=p_actor_real_auth_user_id) then\n    raise exception 'AUTH_ACTOR_REQUIRED' using errcode='42501';\n  end if;\n  select * into v_affiliate from public.affiliates where id=p_affiliate_id for update;\n  if v_affiliate.id is null or v_affiliate.financial_profile_version<>p_expected_profile_version then\n    raise exception 'CONDITIONS_CHANGED' using errcode='PT409';\n  end if;\n  if p_impersonation_session_id is null then\n    if v_affiliate.auth_user_id is distinct from p_actor_real_auth_user_id then raise exception 'AFFILIATE_CONTEXT_DENIED' using errcode='42501'; end if;\n  else\n    select * into v_ctx from public.impersonation_sessions where id=p_impersonation_session_id for share;\n    if v_ctx.id is null or v_ctx.actor_real_auth_user_id<>p_actor_real_auth_user_id\n       or v_ctx.usuario_contexto_affiliate_id<>p_affiliate_id or v_ctx.ended_at is not null or v_ctx.expires_at<=now()\n       or not exists(select 1 from public.admin_assignments a join public.admin_roles r on r.id=a.role_id and r.enabled where a.auth_user_id=p_actor_real_auth_user_id and a.enabled) then\n      raise exception 'IMPERSONATION_CONTEXT_INVALID' using errcode='42501';\n    end if;\n  end if;\n  select * into v_item from public.program_catalog_items\n   where id=p_program_item_id and program_key='prestamo' and enabled and request_mode='supabase' and legacy_boundary;\n  if v_item.id is null then raise exception 'PROGRAM_NOT_REQUESTABLE' using errcode='22023'; end if;\n  if p_idempotency_key is null then raise exception 'IDEMPOTENCY_KEY_REQUIRED' using errcode='22023'; end if;\n  if nullif(btrim(coalesce(p_signature_data,'')),'') is null then raise exception 'SIGNATURE_AND_TERMS_REQUIRED' using errcode='22023'; end if;\n  if p_amount is null or p_amount<=0 or p_term is null or p_term<=0 or length(btrim(coalesce(p_term_semantics,''))) not between 3 and 80 then\n    raise exception 'FINANCIAL_REQUEST_TERMS_INVALID' using errcode='22023';\n  end if;\n  if not exists(select 1 from public.program_terms_versions where id=p_terms_version_id and program_id='prestamo' and membership_offering_id is null and published) then\n    raise exception 'TERMS_VERSION_REQUIRED' using errcode='22023';\n  end if;\n  if p_financial_submission_snapshot is null\n     or jsonb_typeof(p_financial_submission_snapshot)<>'object'\n     or not(p_financial_submission_snapshot ?& array[\n       'affiliate_id','actor_real_auth_user_id','profile_version','profile_fingerprint',\n       'criteria_source_fingerprint','term_policy_fingerprint','calculation_contract_version',\n       'criterion_identity','financialResult','confirmed_at'\n     ])\n     or jsonb_typeof(p_financial_submission_snapshot->'financialResult')<>'object'\n     or p_financial_submission_snapshot->>'affiliate_id'<>p_affiliate_id::text\n     or p_financial_submission_snapshot->>'actor_real_auth_user_id'<>p_actor_real_auth_user_id::text\n     or coalesce(p_financial_submission_snapshot->>'impersonation_session_id','')<>coalesce(p_impersonation_session_id::text,'')\n     or (p_financial_submission_snapshot->>'profile_version')::integer<>p_expected_profile_version\n     or (p_financial_submission_snapshot->'financialResult'->>'amount')::numeric<>p_amount\n     or (p_financial_submission_snapshot->'financialResult'->>'paymentCount')::integer<>p_term then\n    raise exception 'FINANCIAL_SUBMISSION_CONTRACT_MISMATCH' using errcode='22023';\n  end if;\n  select count(*) into v_missing from public.program_document_requirements r\n   where r.program_id='prestamo' and r.membership_offering_id is null and r.enabled and r.required and not exists(\n     select 1 from public.affiliate_documents d where d.id=any(coalesce(p_document_ids,array[]::uuid[]))\n       and d.affiliate_id=p_affiliate_id and d.document_type_id=r.document_type_id\n       and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED'));\n  if v_missing>0 then raise exception 'REQUIRED_DOCUMENTS_MISSING' using errcode='22023'; end if;\n\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_id<>'prestamo'\n       or v_row.program_item_id<>p_program_item_id or v_row.requested_amount<>p_amount or v_row.requested_term<>p_term then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n\n  insert into public.program_requests(\n    actor_real_auth_user_id,affiliate_id,usuario_contexto_affiliate_id,impersonation_session_id,impersonation_reason,\n    numero_control,program_id,program_item_id,request_type,status,quantity,notes,signature_data,terms_accepted,\n    terms_version_id,source_context,financial_processing_status,idempotency_key,requested_amount,requested_term,\n    requested_term_semantics,financial_submission_snapshot\n  ) values(\n    p_actor_real_auth_user_id,p_affiliate_id,case when v_ctx.id is null then null else p_affiliate_id end,v_ctx.id,v_ctx.reason,\n    v_affiliate.numero_control,'prestamo',v_item.id,'quote','requires_financial_processing',1,\n    left(nullif(btrim(coalesce(p_notes,'')),''),2000),p_signature_data,true,p_terms_version_id,\n    jsonb_build_object('source','sutiapp','financial_confirmation','GOOGLE_REVALIDATED','assisted',v_ctx.id is not null),\n    'pending',p_idempotency_key,p_amount,p_term,btrim(p_term_semantics),p_financial_submission_snapshot\n  ) returning * into v_row;\n\n  insert into public.request_documents(request_id,document_type_id,affiliate_document_id,private_asset_id,asset_sha256,status_at_submission)\n  select v_row.id,d.document_type_id,d.id,coalesce(d.private_asset_id,af.private_asset_id),pa.content_sha256,d.status\n  from public.affiliate_documents d left join public.affiliate_files af on af.id=d.affiliate_file_id\n  join public.private_assets pa on pa.id=coalesce(d.private_asset_id,af.private_asset_id)\n  where d.id=any(coalesce(p_document_ids,array[]::uuid[])) and d.affiliate_id=p_affiliate_id\n    and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED')\n  on conflict(request_id,document_type_id) do nothing;\n  return v_row;\nexception when unique_violation then\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_id<>'prestamo'\n       or v_row.program_item_id<>p_program_item_id or v_row.requested_amount<>p_amount or v_row.requested_term<>p_term then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n  raise;\nend $function$\n"},{"signature":"public.create_validated_program_product_payment_request(uuid,uuid,uuid,uuid,text,text,uuid,uuid[],uuid,numeric,integer,integer,date)","owner":"postgres","acl":"{postgres=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"2e35509b6a7bfcf1e17800bf6411d607","result":"81727c5e371213c310c2e6a1b7a3cb4c","definition":"CREATE OR REPLACE FUNCTION public.create_validated_program_product_payment_request(p_actor_real_auth_user_id uuid, p_affiliate_id uuid, p_impersonation_session_id uuid, p_program_item_id uuid, p_notes text, p_signature_data text, p_terms_version_id uuid, p_document_ids uuid[], p_idempotency_key uuid, p_down_payment numeric, p_term integer, p_expected_profile_version integer, p_schedule_anchor_date date)\n RETURNS program_requests\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare\n  v_affiliate public.affiliates%rowtype;\n  v_item public.program_catalog_items%rowtype;\n  v_ctx public.impersonation_sessions%rowtype;\n  v_quote public.program_requests%rowtype;\n  v_row public.program_requests%rowtype;\n  v_policy public.loan_term_policy%rowtype;\n  v_rules jsonb;\n  v_rule jsonb;\n  v_rule_count integer;\n  v_price numeric(14,2);\n  v_amount numeric(14,2);\n  v_source text;\n  v_result jsonb;\n  v_schedule jsonb;\n  v_process text;\n  v_period text;\n  v_union_label text;\n  v_category_label text;\n  v_missing integer;\n  v_snapshot jsonb;\n  v_business_date date:=(now() at time zone 'America/Hermosillo')::date;\nbegin\n  if coalesce(auth.role(),'')<>'service_role' then raise exception 'SERVICE_ROLE_REQUIRED' using errcode='42501'; end if;\n  if p_actor_real_auth_user_id is null or not exists(select 1 from auth.users where id=p_actor_real_auth_user_id) then\n    raise exception 'AUTH_ACTOR_REQUIRED' using errcode='42501';\n  end if;\n  select * into v_affiliate from public.affiliates where id=p_affiliate_id for update;\n  if v_affiliate.id is null or v_affiliate.financial_profile_version<>p_expected_profile_version then\n    raise exception 'CONDITIONS_CHANGED' using errcode='PT409';\n  end if;\n  if p_impersonation_session_id is null then\n    if v_affiliate.auth_user_id is distinct from p_actor_real_auth_user_id then raise exception 'AFFILIATE_CONTEXT_DENIED' using errcode='42501'; end if;\n  else\n    select * into v_ctx from public.impersonation_sessions where id=p_impersonation_session_id for share;\n    if v_ctx.id is null or v_ctx.actor_real_auth_user_id<>p_actor_real_auth_user_id\n       or v_ctx.usuario_contexto_affiliate_id<>p_affiliate_id or v_ctx.ended_at is not null or v_ctx.expires_at<=now()\n       or not exists(select 1 from public.admin_assignments a join public.admin_roles r on r.id=a.role_id and r.enabled\n         where a.auth_user_id=p_actor_real_auth_user_id and a.enabled) then\n      raise exception 'IMPERSONATION_CONTEXT_INVALID' using errcode='42501';\n    end if;\n  end if;\n  select * into v_item from public.program_catalog_items where id=p_program_item_id and enabled and request_mode='supabase' for share;\n  if v_item.id is null or v_item.program_key='prestamo' then raise exception 'PROGRAM_PRODUCT_NOT_FINANCEABLE' using errcode='22023'; end if;\n  if v_item.requires_quote then\n    select * into v_quote from public.program_requests\n    where affiliate_id=p_affiliate_id and program_item_id=v_item.id and request_type='quote' and status='approved'\n      and quoted_amount>0 and (valid_until is null or valid_until>=v_business_date)\n    order by responded_at desc nulls last,created_at desc,id desc limit 1 for share;\n    if v_quote.id is null then raise exception 'AUTHORIZED_PRODUCT_PRICE_UNAVAILABLE' using errcode='P0001'; end if;\n    v_price:=v_quote.quoted_amount;v_source:='APPROVED_QUOTE';\n  else\n    if v_item.price_cash is null or v_item.price_cash<=0 then raise exception 'AUTHORIZED_PRODUCT_PRICE_UNAVAILABLE' using errcode='P0001'; end if;\n    v_price:=v_item.price_cash;v_source:='PRICE_CASH';\n  end if;\n  if p_down_payment is null or p_down_payment<0 or p_down_payment>=v_price then raise exception 'DOWN_PAYMENT_OUT_OF_RANGE' using errcode='22023'; end if;\n  v_amount:=round(v_price-p_down_payment,2);\n  select * into v_policy from public.loan_term_policy where id='primary' and enabled;\n  if v_policy.id is null then raise exception 'LOAN_TERM_POLICY_UNAVAILABLE' using errcode='P0001'; end if;\n  select u.label,c.label into v_union_label,v_category_label\n  from public.affiliates a\n  left join public.segmentation_catalog_entries u on u.catalog_type='union' and u.code=a.financial_union_code and u.enabled\n  left join public.segmentation_catalog_entries c on c.catalog_type='employment_category' and c.code=a.financial_employee_category_code and c.enabled\n  where a.id=v_affiliate.id;\n  if v_union_label is null or v_category_label is null then raise exception 'AFFILIATE_FINANCIAL_PROFILE_INCOMPLETE' using errcode='P0001'; end if;\n  v_rules:=public.get_financial_runtime_rules();\n  select count(*) into v_rule_count\n  from jsonb_array_elements(v_rules) candidate(value)\n  where value->>'program_id'='caja'\n    and public.normalize_suti_financial_key(value->>'category')=public.normalize_suti_financial_key(v_category_label)\n    and public.normalize_suti_financial_key(value->>'union')=public.normalize_suti_financial_key(v_union_label)\n    and case upper(coalesce(value->>'visibility_mode','AUTO'))\n      when 'MOSTRAR' then true when 'OCULTAR' then false\n      else value->>'available_on' is null or (value->>'available_on')::date between v_business_date\n        and (date_trunc('month',v_business_date)+interval '5 months - 1 day')::date end;\n  if v_rule_count<>1 then raise exception 'CAJA_CHICA_RULE_NOT_ELIGIBLE' using errcode='P0001'; end if;\n  select value into v_rule\n  from jsonb_array_elements(v_rules) candidate(value)\n  where value->>'program_id'='caja'\n    and public.normalize_suti_financial_key(value->>'category')=public.normalize_suti_financial_key(v_category_label)\n    and public.normalize_suti_financial_key(value->>'union')=public.normalize_suti_financial_key(v_union_label)\n    and case upper(coalesce(value->>'visibility_mode','AUTO'))\n      when 'MOSTRAR' then true when 'OCULTAR' then false\n      else value->>'available_on' is null or (value->>'available_on')::date between v_business_date\n        and (date_trunc('month',v_business_date)+interval '5 months - 1 day')::date end\n  limit 1;\n  v_rule:=v_rule||jsonb_build_object('status','AVAILABLE');\n  v_result:=public.resolve_suti_loan_quote_contract(\n    jsonb_build_array(v_rule),v_union_label,v_category_label,\n    v_rule->>'id',v_amount,p_term,jsonb_build_object(\n      'source','SUPABASE_LOAN_TERM_POLICY','standardTerms',v_policy.standard_terms,\n      'customMinTerm',v_policy.custom_min_term,'customStep',v_policy.custom_step,\n      'decisionReference',v_policy.decision_reference\n    )\n  );\n  v_process:=case v_affiliate.financial_employee_category_code\n    when 'SUPLENTES_VARIABLES' then '3' when 'JUBILADOS_PENSIONADOS' then 'JUB'\n    when 'SUPLENTES_FIJOS' then '1' when 'EVENTUALES' then '1' when 'BASE' then '1'\n    else null end;\n  if v_process is null then raise exception 'PAYROLL_PROCESS_UNRESOLVED' using errcode='P0001'; end if;\n  v_period:=case when v_process='JUB' then 'mensual' else 'quincenal' end;\n  v_result:=(v_result||jsonb_build_object('paymentPeriod',v_period,'ratePeriod',v_period,'source','SUPABASE_FINANCIAL_CRITERIA'));\n  if p_schedule_anchor_date is distinct from v_business_date then raise exception 'SCHEDULE_ANCHOR_CHANGED' using errcode='PT409'; end if;\n  v_schedule:=public.generate_program_product_payment_schedule(\n    p_schedule_anchor_date,v_process,(v_result->>'paymentCount')::integer,\n    (v_result->>'total')::numeric,(v_result->>'paymentPerPeriod')::numeric\n  );\n  if p_idempotency_key is null then raise exception 'IDEMPOTENCY_KEY_REQUIRED' using errcode='22023'; end if;\n  if nullif(btrim(coalesce(p_signature_data,'')),'') is null then raise exception 'SIGNATURE_AND_TERMS_REQUIRED' using errcode='22023'; end if;\n  if not exists(select 1 from public.program_terms_versions where id=p_terms_version_id and program_id='prestamo'\n    and membership_offering_id is null and published) then raise exception 'TERMS_VERSION_REQUIRED' using errcode='22023'; end if;\n  select count(*) into v_missing\n  from public.resolve_effective_document_requirements('PROGRAM','prestamo') requirement\n  where requirement.required and not exists(\n    select 1 from public.affiliate_documents d\n    left join public.affiliate_files af on af.id=d.affiliate_file_id\n    join public.private_assets pa on pa.id=coalesce(d.private_asset_id,af.private_asset_id) and pa.status='READY'\n    join storage.objects so on so.bucket_id=pa.storage_bucket and so.name=pa.storage_path\n    where d.id=any(coalesce(p_document_ids,array[]::uuid[])) and d.affiliate_id=p_affiliate_id\n      and d.document_type_id=requirement.document_type_id and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED')\n      and not exists(select 1 from public.affiliate_documents newer where newer.affiliate_id=d.affiliate_id\n        and newer.document_type_id=d.document_type_id and (newer.created_at,newer.id)>(d.created_at,d.id))\n  );\n  if v_missing>0 then raise exception 'REQUIRED_DOCUMENTS_MISSING' using errcode='22023'; end if;\n  v_snapshot:=jsonb_build_object(\n    'contract_version','PROGRAM_PRODUCT_PAYMENT_V1','calculation_contract_version','SUTI_LOAN_QUOTE_V1',\n    'calendar_contract_version','PROGRAM_PRODUCT_PAYROLL_CALENDAR_V1','affiliate_id',p_affiliate_id,\n    'actor_real_auth_user_id',p_actor_real_auth_user_id,'impersonation_session_id',p_impersonation_session_id,\n    'profile_version',v_affiliate.financial_profile_version,'product',jsonb_build_object(\n      'program_item_id',v_item.id,'program_key',v_item.program_key,'name',v_item.name\n    ),'authorized_price',v_price,'price_source',v_source,'quote_request_id',v_quote.id,\n    'down_payment',round(p_down_payment,2),'financed_amount',v_amount,'fund_program_id','caja',\n    'fund_rule_id',v_rule->>'rule_id','criterion_identity',v_rule->>'criterion_identity',\n    'financial_authority','SUPABASE','financial_rules_source_hash',v_rule->>'source_snapshot_hash',\n    'financialResult',v_result,'payment_schedule',v_schedule,'terms_version_id',p_terms_version_id,\n    'confirmed_at',now()\n  );\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null then\n    if v_row.actor_real_auth_user_id<>p_actor_real_auth_user_id or v_row.program_item_id<>p_program_item_id\n       or v_row.requested_amount<>v_amount or v_row.requested_term<>p_term\n       or (v_row.financial_submission_snapshot->>'down_payment')::numeric<>round(p_down_payment,2)\n       or v_row.financial_submission_snapshot->>'price_source'<>v_source\n       or coalesce(v_row.financial_submission_snapshot->>'quote_request_id','')<>coalesce(v_quote.id::text,'') then\n      raise exception 'IDEMPOTENCY_CONTRACT_MISMATCH' using errcode='22023';\n    end if;\n    return v_row;\n  end if;\n  insert into public.program_requests(\n    actor_real_auth_user_id,affiliate_id,usuario_contexto_affiliate_id,impersonation_session_id,impersonation_reason,\n    numero_control,program_id,program_item_id,request_type,status,quantity,notes,signature_data,terms_accepted,\n    terms_version_id,source_context,financial_processing_status,idempotency_key,requested_amount,requested_term,\n    requested_term_semantics,financial_submission_snapshot\n  ) values(\n    p_actor_real_auth_user_id,p_affiliate_id,case when v_ctx.id is null then null else p_affiliate_id end,v_ctx.id,v_ctx.reason,\n    v_affiliate.numero_control,v_item.program_key,v_item.id,'benefit','requires_financial_processing',1,\n    left(nullif(btrim(coalesce(p_notes,'')),''),2000),p_signature_data,true,p_terms_version_id,\n    jsonb_build_object('source','sutiapp','flow','PROGRAM_PRODUCT_PAYMENT_V1','price_source',v_source,\n      'quote_request_id',v_quote.id,'fund_program_id','caja','assisted',v_ctx.id is not null),\n    'pending',p_idempotency_key,v_amount,p_term,v_period,v_snapshot\n  ) returning * into v_row;\n  insert into public.request_documents(request_id,document_type_id,affiliate_document_id,private_asset_id,asset_sha256,status_at_submission)\n  select v_row.id,d.document_type_id,d.id,coalesce(d.private_asset_id,af.private_asset_id),pa.content_sha256,d.status\n  from public.affiliate_documents d left join public.affiliate_files af on af.id=d.affiliate_file_id\n  join public.private_assets pa on pa.id=coalesce(d.private_asset_id,af.private_asset_id)\n  join storage.objects so on so.bucket_id=pa.storage_bucket and so.name=pa.storage_path\n  where d.id=any(coalesce(p_document_ids,array[]::uuid[])) and d.affiliate_id=p_affiliate_id\n    and d.status in('PENDING_REVIEW','UNDER_REVIEW','VERIFIED')\n  on conflict(request_id,document_type_id) do nothing;\n  insert into public.sensitive_change_audit(actor_auth_user_id,affiliate_id,resource,action,target_id,metadata)\n  values(p_actor_real_auth_user_id,p_affiliate_id,'program_requests','PROGRAM_PRODUCT_PAYMENT_CONFIRMED',v_row.id,\n    jsonb_build_object('program_item_id',v_item.id,'price_source',v_source,'quote_request_id',v_quote.id,\n      'payment_count',p_term,'payment_period',v_period,'assisted',v_ctx.id is not null));\n  return v_row;\nexception when unique_violation then\n  select * into v_row from public.program_requests where affiliate_id=p_affiliate_id and idempotency_key=p_idempotency_key;\n  if v_row.id is not null and v_row.program_item_id=p_program_item_id and v_row.requested_amount=v_amount\n     and v_row.requested_term=p_term and (v_row.financial_submission_snapshot->>'down_payment')::numeric=round(p_down_payment,2) then\n    return v_row;\n  end if;\n  raise;\nend $function$\n"},{"signature":"public.restore_admin_affiliate(uuid,timestamp with time zone,text)","owner":"postgres","acl":"{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"0b8238a17593901b76a6f1ac75bfc6e2","result":"19bf8d0ce97e05f79b3d9574ae058191","definition":"CREATE OR REPLACE FUNCTION public.restore_admin_affiliate(p_affiliate_id uuid, p_expected_updated_at timestamp with time zone, p_reason text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare v_old public.affiliates%rowtype;v_new public.affiliates%rowtype;v_reason text:=btrim(coalesce(p_reason,''));v_email text;v_eligibility text;\nbegin\n  if not public.has_admin_permission('affiliates.write') then raise exception 'AFFILIATE_WRITE_DENIED' using errcode='42501'; end if;\n  if length(v_reason) not between 8 and 500 then raise exception 'AFFILIATE_RESTORE_REASON_REQUIRED' using errcode='22023'; end if;\n  select * into v_old from public.affiliates where id=p_affiliate_id for update;\n  if v_old.id is null then raise exception 'AFFILIATE_NOT_FOUND' using errcode='P0001'; end if;\n  if v_old.updated_at is distinct from p_expected_updated_at then raise exception 'AFFILIATE_VERSION_CONFLICT' using errcode='PT409'; end if;\n  if not v_old.is_archived then raise exception 'AFFILIATE_NOT_ARCHIVED' using errcode='22023'; end if;\n  v_email:=v_old.historical_email_normalized;\n  v_eligibility:=case when v_email is null then 'missing_email'\n    when v_email!~ '^[^@[:space:]]+@[^@[:space:]]+[.][^@[:space:]]+$' then 'invalid_email'\n    when exists(select 1 from public.affiliates a where a.id<>p_affiliate_id and a.historical_email_normalized=v_email) then 'duplicate_email'\n    else 'eligible' end;\n  update public.affiliates set is_archived=false,archived_at=null,archived_by_auth_user_id=null,\n    archive_reason=null,archive_previous_status_raw=null,restored_at=now(),\n    restored_by_auth_user_id=auth.uid(),restore_reason=v_reason,auth_eligibility=v_eligibility,\n    auth_ineligibility_reason=case when v_eligibility='eligible' then null else v_eligibility end\n  where id=p_affiliate_id returning * into v_new;\n  insert into public.affiliate_admin_events(affiliate_id,actor_auth_user_id,action,before_values,after_values,changed_fields,reason)\n  values(p_affiliate_id,auth.uid(),'RESTORE',\n    jsonb_build_object('is_archived',true,'archived_at',v_old.archived_at,'archive_previous_status_raw',v_old.archive_previous_status_raw),\n    jsonb_build_object('is_archived',false,'restored_at',v_new.restored_at,'affiliate_status_raw',v_new.affiliate_status_raw,'auth_eligibility',v_new.auth_eligibility),\n    array['is_archived','restored_at','restored_by_auth_user_id','restore_reason','auth_eligibility'],v_reason);\n  return public.get_admin_affiliate_workbench(p_affiliate_id);\nend $function$\n"},{"signature":"public.save_current_declared_payroll(numeric,numeric,integer)","owner":"postgres","acl":"{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"67b17ea65b433cdfd9e190ffd1627a68","result":"a372f5d9b5287d679d000c4369761b1a","definition":"CREATE OR REPLACE FUNCTION public.save_current_declared_payroll(p_gross_pay_per_fortnight numeric, p_deductions_per_fortnight numeric, p_expected_version integer DEFAULT NULL::integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare\n  v_actor uuid:=auth.uid(); v_affiliate_id uuid:=public.get_effective_affiliate_id();\n  v_existing public.affiliate_payroll_declarations%rowtype; v_saved public.affiliate_payroll_declarations%rowtype;\n  v_ctx public.impersonation_sessions%rowtype;\nbegin\n  if v_actor is null then raise exception 'AUTH_REQUIRED' using errcode='42501'; end if;\n  if v_affiliate_id is null then raise exception 'AFFILIATE_IDENTITY_REQUIRED' using errcode='42501'; end if;\n  select * into v_ctx from public.impersonation_sessions where actor_real_auth_user_id=v_actor and ended_at is null and expires_at>now() limit 1;\n  if v_ctx.id is not null and (not public.is_active_admin() or v_ctx.usuario_contexto_affiliate_id is distinct from v_affiliate_id) then\n    raise exception 'PAYROLL_DECLARATION_IMPERSONATION_DENIED' using errcode='42501';\n  end if;\n  if p_gross_pay_per_fortnight is null or p_gross_pay_per_fortnight<=0 or p_gross_pay_per_fortnight>1000000 or\n     p_deductions_per_fortnight is null or p_deductions_per_fortnight<0 or p_deductions_per_fortnight>=p_gross_pay_per_fortnight then\n    raise exception 'PAYROLL_DECLARATION_INVALID' using errcode='22023';\n  end if;\n  select * into v_existing from public.affiliate_payroll_declarations where affiliate_id=v_affiliate_id for update;\n  if v_existing.affiliate_id is null then\n    if coalesce(p_expected_version,0)<>0 then raise exception 'PAYROLL_DECLARATION_VERSION_CONFLICT' using errcode='PT409'; end if;\n    insert into public.affiliate_payroll_declarations(affiliate_id,gross_pay_per_fortnight,deductions_per_fortnight,updated_by_auth_user_id)\n    values(v_affiliate_id,round(p_gross_pay_per_fortnight,2),round(p_deductions_per_fortnight,2),v_actor) returning * into v_saved;\n  else\n    if p_expected_version is null or p_expected_version<>v_existing.version then raise exception 'PAYROLL_DECLARATION_VERSION_CONFLICT' using errcode='PT409'; end if;\n    update public.affiliate_payroll_declarations set gross_pay_per_fortnight=round(p_gross_pay_per_fortnight,2),\n      deductions_per_fortnight=round(p_deductions_per_fortnight,2),version=version+1,updated_at=now(),updated_by_auth_user_id=v_actor\n      where affiliate_id=v_affiliate_id returning * into v_saved;\n  end if;\n  insert into public.affiliate_payroll_declaration_audit(\n    affiliate_id,actor_real_auth_user_id,action,old_gross_pay_per_fortnight,old_deductions_per_fortnight,\n    new_gross_pay_per_fortnight,new_deductions_per_fortnight,declaration_version,\n    usuario_contexto_affiliate_id,impersonation_session_id,impersonation_reason\n  ) values(v_affiliate_id,v_actor,case when v_existing.affiliate_id is null then 'INSERT' else 'UPDATE' end,\n    v_existing.gross_pay_per_fortnight,v_existing.deductions_per_fortnight,v_saved.gross_pay_per_fortnight,\n    v_saved.deductions_per_fortnight,v_saved.version,case when v_ctx.id is null then null else v_affiliate_id end,v_ctx.id,v_ctx.reason);\n  return public.get_current_declared_payroll();\nend $function$\n"},{"signature":"public.update_admin_affiliate(uuid,timestamp with time zone,jsonb,text)","owner":"postgres","acl":"{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"1d8781cb6d5ea8e8f4b34082e414b244","result":"f145088ca0cf71c18151e342ee9abc82","definition":"CREATE OR REPLACE FUNCTION public.update_admin_affiliate(p_affiliate_id uuid, p_expected_updated_at timestamp with time zone, p_patch jsonb, p_reason text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare\n  v_old public.affiliates%rowtype;v_new public.affiliates%rowtype;v_key text;v_value text;v_reason text:=btrim(coalesce(p_reason,''));\n  v_allowed constant text[]:=array['full_name','display_name','historical_email_raw','phone_raw','address_raw','birth_date_raw','gender_raw','marital_status_raw','children_count_raw','rfc_raw','curp_raw','unit_raw','city_raw','employment_position_raw','employment_entry_date_raw','occupation_raw','institute_entry_date_raw','employment_area_raw','employment_level_raw','pension_raw','subdirectorate_raw','union_enrollment_date_raw','capture_date_raw','affiliation_raw','union_position_raw','termination_date_raw','financial_union_code','financial_employee_category_code','financial_employee_type','financial_affiliation_status','financial_employment_status'];\n  v_profile_fields constant text[]:=array['full_name','display_name','phone_raw','unit_raw','city_raw','employment_position_raw','employment_area_raw','financial_union_code','financial_employee_category_code','financial_employee_type','financial_affiliation_status','financial_employment_status'];\n  v_changed text[]:=array[]::text[];v_before jsonb:='{}'::jsonb;v_after jsonb:='{}'::jsonb;v_profile_change boolean:=false;v_email text;v_eligibility text;\nbegin\n  if not public.has_admin_permission('affiliates.write') then raise exception 'AFFILIATE_WRITE_DENIED' using errcode='42501'; end if;\n  if jsonb_typeof(p_patch)<>'object' or p_patch='{}'::jsonb then raise exception 'AFFILIATE_PATCH_REQUIRED' using errcode='22023'; end if;\n  if length(v_reason) not between 8 and 500 then raise exception 'AFFILIATE_REASON_REQUIRED' using errcode='22023'; end if;\n  select * into v_old from public.affiliates where id=p_affiliate_id for update;\n  if v_old.id is null then raise exception 'AFFILIATE_NOT_FOUND' using errcode='P0001'; end if;\n  if v_old.updated_at is distinct from p_expected_updated_at then raise exception 'AFFILIATE_VERSION_CONFLICT' using errcode='PT409'; end if;\n  for v_key in select jsonb_object_keys(p_patch) loop\n    if not(v_key=any(v_allowed)) then raise exception 'AFFILIATE_FIELD_DENIED: %',v_key using errcode='22023'; end if;\n    v_value:=nullif(btrim(p_patch->>v_key),'');\n    if v_key in ('rfc_raw','curp_raw') then v_value:=upper(v_value); end if;\n    if to_jsonb(v_old)->>v_key is distinct from v_value then\n      v_changed:=array_append(v_changed,v_key);v_before:=v_before||jsonb_build_object(v_key,to_jsonb(v_old)->v_key);v_after:=v_after||jsonb_build_object(v_key,to_jsonb(v_value));\n      if v_key=any(v_profile_fields) then v_profile_change:=true; end if;\n    end if;\n  end loop;\n  if cardinality(v_changed)=0 then raise exception 'AFFILIATE_NO_CHANGE' using errcode='22023'; end if;\n  if p_patch?'rfc_raw' and nullif(upper(btrim(p_patch->>'rfc_raw')),'') is not null and exists(select 1 from public.affiliates where id<>p_affiliate_id and upper(btrim(rfc_raw))=upper(btrim(p_patch->>'rfc_raw'))) then raise exception 'AFFILIATE_RFC_DUPLICATE' using errcode='23505'; end if;\n  if p_patch?'curp_raw' and nullif(upper(btrim(p_patch->>'curp_raw')),'') is not null and exists(select 1 from public.affiliates where id<>p_affiliate_id and upper(btrim(curp_raw))=upper(btrim(p_patch->>'curp_raw'))) then raise exception 'AFFILIATE_CURP_DUPLICATE' using errcode='23505'; end if;\n  if p_patch?'financial_union_code' and nullif(btrim(p_patch->>'financial_union_code'),'') is not null and not exists(select 1 from public.segmentation_catalog_entries where catalog_type='union' and code=p_patch->>'financial_union_code' and enabled) then raise exception 'AFFILIATE_UNION_INVALID' using errcode='22023'; end if;\n  if p_patch?'financial_employee_category_code' and nullif(btrim(p_patch->>'financial_employee_category_code'),'') is not null and not exists(select 1 from public.segmentation_catalog_entries where catalog_type='employment_category' and code=p_patch->>'financial_employee_category_code' and enabled) then raise exception 'AFFILIATE_CATEGORY_INVALID' using errcode='22023'; end if;\n  v_email:=case when p_patch?'historical_email_raw' then nullif(lower(btrim(p_patch->>'historical_email_raw')),'') else v_old.historical_email_normalized end;\n  v_eligibility:=case when v_old.auth_user_id is not null then v_old.auth_eligibility when v_email is null then 'missing_email'\n    when v_email!~ '^[^@[:space:]]+@[^@[:space:]]+[.][^@[:space:]]+$' then 'invalid_email'\n    when exists(select 1 from public.affiliates where id<>p_affiliate_id and historical_email_normalized=v_email) then 'duplicate_email' else 'eligible' end;\n  update public.affiliates set\n    full_name=case when p_patch?'full_name' then nullif(btrim(p_patch->>'full_name'),'') else full_name end,\n    display_name=case when p_patch?'display_name' then nullif(btrim(p_patch->>'display_name'),'') else display_name end,\n    historical_email_raw=case when p_patch?'historical_email_raw' then nullif(btrim(p_patch->>'historical_email_raw'),'') else historical_email_raw end,\n    historical_email_normalized=case when p_patch?'historical_email_raw' then v_email else historical_email_normalized end,\n    phone_raw=case when p_patch?'phone_raw' then nullif(btrim(p_patch->>'phone_raw'),'') else phone_raw end,\n    address_raw=case when p_patch?'address_raw' then nullif(btrim(p_patch->>'address_raw'),'') else address_raw end,\n    birth_date_raw=case when p_patch?'birth_date_raw' then nullif(btrim(p_patch->>'birth_date_raw'),'') else birth_date_raw end,\n    gender_raw=case when p_patch?'gender_raw' then nullif(btrim(p_patch->>'gender_raw'),'') else gender_raw end,\n    marital_status_raw=case when p_patch?'marital_status_raw' then nullif(btrim(p_patch->>'marital_status_raw'),'') else marital_status_raw end,\n    children_count_raw=case when p_patch?'children_count_raw' then nullif(btrim(p_patch->>'children_count_raw'),'') else children_count_raw end,\n    rfc_raw=case when p_patch?'rfc_raw' then nullif(upper(btrim(p_patch->>'rfc_raw')),'') else rfc_raw end,\n    curp_raw=case when p_patch?'curp_raw' then nullif(upper(btrim(p_patch->>'curp_raw')),'') else curp_raw end,\n    unit_raw=case when p_patch?'unit_raw' then nullif(btrim(p_patch->>'unit_raw'),'') else unit_raw end,\n    city_raw=case when p_patch?'city_raw' then nullif(btrim(p_patch->>'city_raw'),'') else city_raw end,\n    employment_position_raw=case when p_patch?'employment_position_raw' then nullif(btrim(p_patch->>'employment_position_raw'),'') else employment_position_raw end,\n    employment_entry_date_raw=case when p_patch?'employment_entry_date_raw' then nullif(btrim(p_patch->>'employment_entry_date_raw'),'') else employment_entry_date_raw end,\n    occupation_raw=case when p_patch?'occupation_raw' then nullif(btrim(p_patch->>'occupation_raw'),'') else occupation_raw end,\n    institute_entry_date_raw=case when p_patch?'institute_entry_date_raw' then nullif(btrim(p_patch->>'institute_entry_date_raw'),'') else institute_entry_date_raw end,\n    employment_area_raw=case when p_patch?'employment_area_raw' then nullif(btrim(p_patch->>'employment_area_raw'),'') else employment_area_raw end,\n    employment_level_raw=case when p_patch?'employment_level_raw' then nullif(btrim(p_patch->>'employment_level_raw'),'') else employment_level_raw end,\n    pension_raw=case when p_patch?'pension_raw' then nullif(btrim(p_patch->>'pension_raw'),'') else pension_raw end,\n    subdirectorate_raw=case when p_patch?'subdirectorate_raw' then nullif(btrim(p_patch->>'subdirectorate_raw'),'') else subdirectorate_raw end,\n    union_enrollment_date_raw=case when p_patch?'union_enrollment_date_raw' then nullif(btrim(p_patch->>'union_enrollment_date_raw'),'') else union_enrollment_date_raw end,\n    capture_date_raw=case when p_patch?'capture_date_raw' then nullif(btrim(p_patch->>'capture_date_raw'),'') else capture_date_raw end,\n    affiliation_raw=case when p_patch?'affiliation_raw' then nullif(btrim(p_patch->>'affiliation_raw'),'') else affiliation_raw end,\n    union_position_raw=case when p_patch?'union_position_raw' then nullif(btrim(p_patch->>'union_position_raw'),'') else union_position_raw end,\n    termination_date_raw=case when p_patch?'termination_date_raw' then nullif(btrim(p_patch->>'termination_date_raw'),'') else termination_date_raw end,\n    financial_union_code=case when p_patch?'financial_union_code' then nullif(btrim(p_patch->>'financial_union_code'),'') else financial_union_code end,\n    financial_employee_category_code=case when p_patch?'financial_employee_category_code' then nullif(btrim(p_patch->>'financial_employee_category_code'),'') else financial_employee_category_code end,\n    financial_employee_type=case when p_patch?'financial_employee_type' then nullif(btrim(p_patch->>'financial_employee_type'),'') else financial_employee_type end,\n    financial_affiliation_status=case when p_patch?'financial_affiliation_status' then nullif(btrim(p_patch->>'financial_affiliation_status'),'') else financial_affiliation_status end,\n    financial_employment_status=case when p_patch?'financial_employment_status' then nullif(btrim(p_patch->>'financial_employment_status'),'') else financial_employment_status end,\n    auth_eligibility=case when p_patch?'historical_email_raw' and auth_user_id is null then v_eligibility else auth_eligibility end,\n    auth_ineligibility_reason=case when p_patch?'historical_email_raw' and auth_user_id is null then case when v_eligibility='eligible' then null else v_eligibility end else auth_ineligibility_reason end,\n    financial_profile_version=financial_profile_version+case when v_profile_change then 1 else 0 end,\n    financial_profile_updated_at=case when v_profile_change then now() else financial_profile_updated_at end,\n    financial_profile_updated_by=case when v_profile_change then auth.uid() else financial_profile_updated_by end\n  where id=p_affiliate_id returning * into v_new;\n  insert into public.affiliate_admin_events(affiliate_id,actor_auth_user_id,action,before_values,after_values,changed_fields,reason)\n  values(p_affiliate_id,auth.uid(),'UPDATE',v_before,v_after,v_changed,v_reason);\n  for v_key in select unnest(v_changed) loop\n    if v_key=any(v_profile_fields) then\n      insert into public.affiliate_profile_audit_log(affiliate_id,field_name,old_value,new_value,changed_by,reason,profile_version)\n      values(p_affiliate_id,v_key,v_before->v_key,v_after->v_key,auth.uid(),v_reason,v_new.financial_profile_version);\n    end if;\n  end loop;\n  return public.get_admin_affiliate_workbench(p_affiliate_id);\nend $function$\n"},{"signature":"public.update_affiliate_admin_profile(uuid,integer,jsonb,text)","owner":"postgres","acl":"{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}","prosecdef":true,"provolatile":"v","proparallel":"u","proconfig":["search_path=\"\""],"expected":"0f8abb68c37b6a8690359cff3f94e514","result":"8d88f18260d73bc3d44a58459ee80a2d","definition":"CREATE OR REPLACE FUNCTION public.update_affiliate_admin_profile(p_affiliate_id uuid, p_expected_version integer, p_patch jsonb, p_reason text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\ndeclare v_old public.affiliates%rowtype; v_new public.affiliates%rowtype; v_key text; v_changed boolean:=false; v_allowed constant text[]:=array[\n  'full_name','display_name','phone_raw','unit_raw','city_raw','employment_position_raw','employment_area_raw',\n  'financial_union_code','financial_employee_category_code','financial_employee_type','financial_affiliation_status','financial_employment_status'];\nbegin\n  if not public.has_admin_permission('affiliates.write') then raise exception 'AFFILIATE_WRITE_DENIED' using errcode='42501'; end if;\n  if jsonb_typeof(p_patch)<>'object' or p_patch='{}'::jsonb then raise exception 'PROFILE_PATCH_REQUIRED' using errcode='22023'; end if;\n  if length(btrim(coalesce(p_reason,''))) not between 8 and 500 then raise exception 'PROFILE_REASON_REQUIRED' using errcode='22023'; end if;\n  for v_key in select jsonb_object_keys(p_patch) loop if not(v_key=any(v_allowed)) then raise exception 'PROFILE_FIELD_DENIED: %',v_key using errcode='22023'; end if; end loop;\n  select * into v_old from public.affiliates where id=p_affiliate_id for update;\n  if v_old.id is null then raise exception 'AFFILIATE_NOT_FOUND' using errcode='P0001'; end if;\n  if v_old.financial_profile_version<>coalesce(p_expected_version,-1) then raise exception 'PROFILE_VERSION_CONFLICT' using errcode='PT409'; end if;\n  if p_patch ? 'financial_union_code' and nullif(btrim(p_patch->>'financial_union_code'),'') is not null and not exists(\n    select 1 from public.segmentation_catalog_entries where catalog_type='union' and code=p_patch->>'financial_union_code' and enabled\n  ) then raise exception 'FINANCIAL_UNION_INVALID' using errcode='22023'; end if;\n  if p_patch ? 'financial_employee_category_code' and nullif(btrim(p_patch->>'financial_employee_category_code'),'') is not null and not exists(\n    select 1 from public.segmentation_catalog_entries where catalog_type='employment_category' and code=p_patch->>'financial_employee_category_code' and enabled\n  ) then raise exception 'FINANCIAL_CATEGORY_INVALID' using errcode='22023'; end if;\n  update public.affiliates set\n    full_name=case when p_patch?'full_name' then nullif(btrim(p_patch->>'full_name'),'') else full_name end,\n    display_name=case when p_patch?'display_name' then nullif(btrim(p_patch->>'display_name'),'') else display_name end,\n    phone_raw=case when p_patch?'phone_raw' then nullif(btrim(p_patch->>'phone_raw'),'') else phone_raw end,\n    unit_raw=case when p_patch?'unit_raw' then nullif(btrim(p_patch->>'unit_raw'),'') else unit_raw end,\n    city_raw=case when p_patch?'city_raw' then nullif(btrim(p_patch->>'city_raw'),'') else city_raw end,\n    employment_position_raw=case when p_patch?'employment_position_raw' then nullif(btrim(p_patch->>'employment_position_raw'),'') else employment_position_raw end,\n    employment_area_raw=case when p_patch?'employment_area_raw' then nullif(btrim(p_patch->>'employment_area_raw'),'') else employment_area_raw end,\n    financial_union_code=case when p_patch?'financial_union_code' then nullif(btrim(p_patch->>'financial_union_code'),'') else financial_union_code end,\n    financial_employee_category_code=case when p_patch?'financial_employee_category_code' then nullif(btrim(p_patch->>'financial_employee_category_code'),'') else financial_employee_category_code end,\n    financial_employee_type=case when p_patch?'financial_employee_type' then nullif(btrim(p_patch->>'financial_employee_type'),'') else financial_employee_type end,\n    financial_affiliation_status=case when p_patch?'financial_affiliation_status' then nullif(btrim(p_patch->>'financial_affiliation_status'),'') else financial_affiliation_status end,\n    financial_employment_status=case when p_patch?'financial_employment_status' then nullif(btrim(p_patch->>'financial_employment_status'),'') else financial_employment_status end,\n    financial_profile_version=financial_profile_version+1,financial_profile_updated_at=now(),financial_profile_updated_by=auth.uid(),updated_at=now()\n  where id=p_affiliate_id returning * into v_new;\n  for v_key in select unnest(v_allowed) loop\n    if to_jsonb(v_old)->v_key is distinct from to_jsonb(v_new)->v_key then\n      v_changed:=true;\n      insert into public.affiliate_profile_audit_log(affiliate_id,field_name,old_value,new_value,changed_by,reason,profile_version)\n      values(p_affiliate_id,v_key,to_jsonb(v_old)->v_key,to_jsonb(v_new)->v_key,auth.uid(),btrim(p_reason),v_new.financial_profile_version);\n    end if;\n  end loop;\n  if not v_changed then raise exception 'PROFILE_NO_CHANGE' using errcode='22023'; end if;\n  return public.get_affiliate_admin_profile(p_affiliate_id);\nend $function$\n"}]$h01_targets$::jsonb) LOOP
    SELECT p.*,p.proowner::regrole::text owner_name INTO STRICT proc FROM pg_proc p WHERE p.oid=to_regprocedure(item->>'signature');
    IF proc.owner_name IS DISTINCT FROM item->>'owner' OR proc.proacl::text IS DISTINCT FROM item->>'acl'
      OR proc.prosecdef IS DISTINCT FROM (item->>'prosecdef')::boolean
      OR proc.provolatile::text IS DISTINCT FROM item->>'provolatile'
      OR proc.proparallel::text IS DISTINCT FROM item->>'proparallel'
      OR to_jsonb(proc.proconfig) IS DISTINCT FROM NULLIF(item->'proconfig','null'::jsonb)
    THEN RAISE EXCEPTION 'H01_SECURITY_DRIFT: %',item->>'signature'; END IF;
    current_def:=pg_get_functiondef(proc.oid);
    IF md5(current_def)=item->>'result' THEN CONTINUE; END IF;
    IF md5(current_def) IS DISTINCT FROM item->>'expected' THEN RAISE EXCEPTION 'H01_DEFINITION_DRIFT: %',item->>'signature'; END IF;
    EXECUTE item->>'definition';
    IF md5(pg_get_functiondef(proc.oid)) IS DISTINCT FROM item->>'result' THEN RAISE EXCEPTION 'H01_RESULT_MISMATCH: %',item->>'signature'; END IF;
  END LOOP;
END $h01$;
COMMIT;
